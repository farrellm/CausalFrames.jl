var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"Overview","text":"Every operator and summarizer has its own section on one of the pages below; the links here go straight to it.\n\nCategory Operators\nFrames and pipelines Context, CausalFrame, CausalPipeline, load, stream, scan\nSources emptyframe, concatenate, merge, clock\nFile I/O readcsv, readparquet, readjls, writecsv, writeparquet, writejls\nRow transformations filterrows, addcolumns, head, lastrow, lag, Acausal.lead, settime, Acausal.settime\nColumn transformations selectcolumns, dropcolumns, reordercolumns, forwardfill, fillmissing, asofjoin, Acausal.futurejoin\nSummarizing transforms summarize, summarizecycles, intervalize, summarizewindows, addsummarycolumns, addrollingcolumns\nModel fitting (MLJ) applymodels, addpredictions, modelreports\n\nThe summarizers the summarizing transforms take:\n\nCategory Summarizers\nCounting Count, CountDistinct\nAccumulating Sum, SumPower, Product, DotProduct\nMoments Moment, Mean, Variance, Std\nRelating two columns Covariance, Correlation, LinearRegression\nTracking a single row Min, Max, First, Last\nModel fitting FitModel","title":"API reference"},{"category":"section","location":"api/rows/#Row-transformations","page":"Row transformations","text":"Everything driven by one row at a time: testing a row, computing values from it, and selecting or retiming rows. The column set either stays as it is or grows by what a row function returns.\n\nThe forward-looking time shifts live in the CausalFrames.Acausal submodule and are never re-exported — reach them with using CausalFrames.Acausal.","title":"Row transformations"},{"category":"section","location":"api/rows/#filterrows","page":"Row transformations","text":"","title":"filterrows"},{"category":"section","location":"api/rows/#addcolumns","page":"Row transformations","text":"Neither of the truncating transforms is row-wise: both carry state across the whole window, head a remaining-row budget and lastrow a store of rows per key.","title":"addcolumns"},{"category":"section","location":"api/rows/#head","page":"Row transformations","text":"","title":"head"},{"category":"section","location":"api/rows/#lastrow","page":"Row transformations","text":"","title":"lastrow"},{"category":"section","location":"api/rows/#lag","page":"Row transformations","text":"","title":"lag"},{"category":"section","location":"api/rows/#Acausal.lead","page":"Row transformations","text":"lead shifts forward in time and so is acausal; it lives in the CausalFrames.Acausal submodule and is not re-exported.","title":"Acausal.lead"},{"category":"section","location":"api/rows/#settime","page":"Row transformations","text":"Where lag and lead shift every row by one constant, settime recomputes :time per row, from a column or a function.","title":"settime"},{"category":"section","location":"api/rows/#Acausal.settime","page":"Row transformations","text":"The permissive variant, which may move rows earlier, is acausal and lives in the CausalFrames.Acausal submodule. Unlike futurejoin and lead it is not exported even from there, so that using CausalFrames.Acausal leaves the causal settime unambiguous — reach it as CausalFrames.Acausal.settime.","title":"Acausal.settime"},{"category":"function","location":"api/rows/#CausalFrames.filterrows","page":"Row transformations","text":"filterrows(pred) -> (CausalPipeline -> CausalPipeline)\nfilterrows(p::CausalPipeline, pred) -> CausalPipeline\n\nA transform keeping the rows where pred(row) is true. pred receives a map-like row object supporting row.name and row[:name] access, including row.time.\n\nThe curried form composes with |>; the uncurried form applies directly, so filterrows(p, pred) is equivalent to p |> filterrows(pred).\n\n\n\n\n\n","title":"CausalFrames.filterrows"},{"category":"function","location":"api/rows/#CausalFrames.addcolumns","page":"Row transformations","text":"addcolumns(f) -> (CausalPipeline -> CausalPipeline)\naddcolumns(p::CausalPipeline, f) -> CausalPipeline\n\nA transform adding columns computed row by row: f(row) must return a NamedTuple mapping new column names to that row's values. The returned tuple may not contain a time key. f receives the same map-like row object as filterrows.\n\nThe curried form composes with |>; the uncurried form applies directly, so addcolumns(p, f) is equivalent to p |> addcolumns(f).\n\n\n\n\n\n","title":"CausalFrames.addcolumns"},{"category":"function","location":"api/rows/#CausalFrames.head","page":"Row transformations","text":"head(n) -> (CausalPipeline -> CausalPipeline)\nhead(p::CausalPipeline, n) -> CausalPipeline\n\nA transform emitting the first n rows of the stream and then stopping. It stops for real: once n rows have gone out the upstream pipeline is never pulled again, so readcsv(path; types) |> head(10) reads one file chunk and no more. n must be non-negative (an ArgumentError at construction otherwise), and head(0) is the empty stream — the upstream pipeline is built but never iterated.\n\nhead is causal — rows pass through unchanged and in order — but stateful: its row budget spans the whole window. Concatenating the frames of stream therefore still equals load of the same window, while the chunk-concatenation property over split contexts does not hold, since head(n) over [a, b) and over [b, c) yields up to 2n rows where the whole window yields n.\n\nDo not put a sink upstream of head: writecsv and writeparquet finalize when the stream is exhausted, which head prevents, leaving the file unfinished and its writer task unjoined. Truncate first — p |> head(n) |> writecsv(path).\n\nThe curried form composes with |>; the uncurried form applies directly, so head(p, n) is equivalent to p |> head(n).\n\n\n\n\n\n","title":"CausalFrames.head"},{"category":"function","location":"api/rows/#CausalFrames.lastrow","page":"Row transformations","text":"lastrow(; key = nothing) -> (CausalPipeline -> CausalPipeline)\nlastrow(p::CausalPipeline; key = nothing) -> CausalPipeline\n\nA transform emitting the stream's last row, retimed to the context's end time stop. Every input column is kept, carrying its value from that row: the output schema is the input's exactly, :time included and in its own position.\n\nWithout key the output is exactly one row, and an input with no rows produces no output at all — unlike keyless summarize, which has an identity summary to emit. With key (a column name or collection of column names) one row is emitted per distinct key value, each being that key's last row, all retimed to stop and emitted sorted by key — legal precisely because every emitted time is then equal.\n\nThe original timestamp is lost: :time is overwritten with stop. Carry it forward under another name if you need it:\n\np |> addcolumns(r -> (; t0 = r.time)) |> lastrow(; key = :sym)\n\ntime may not be a key and key columns must be unique — both ArgumentErrors when the transform is built — and the key columns must be present in the input, checked on its first chunk. A chunk whose column names differ from the first chunk's, in content or in order, is an ArgumentError; element types may differ from chunk to chunk, as everywhere else, and the store tracks their promotion.\n\nLike summarize, lastrow is causal — a row emitted at stop folds only rows with time <= stop — and stateful over the whole window, emitting once when its input is exhausted, so its stream is a single frame over [start, stop].\n\nThe curried form composes with |>; the uncurried form applies directly, so lastrow(p; key) is equivalent to p |> lastrow(; key).\n\n\n\n\n\n","title":"CausalFrames.lastrow"},{"category":"function","location":"api/rows/#CausalFrames.lag","page":"Row transformations","text":"lag(offset) -> (CausalPipeline -> CausalPipeline)\nlag(p::CausalPipeline, offset) -> CausalPipeline\n\nA transform shifting every row offset later in time (time -> time + offset), so that the value observed at time t is the one the input carried at t - offset — the lagged, backward-looking view. Only the :time column changes; all other columns pass through unchanged.\n\noffset must be non-negative (a negative shift would look into the future and break causality — see lead for that); an ArgumentError is thrown when the pipeline runs otherwise, and offset == 0 is the identity. The time type must support adding and subtracting the offset (numbers and Dates types do): the upstream pipeline is run over the window [start - offset, stop - offset) so the shifted output covers [start, stop).\n\nThe curried form composes with |>; the uncurried form applies directly, so lag(p, offset) is equivalent to p |> lag(offset).\n\n\n\n\n\n","title":"CausalFrames.lag"},{"category":"function","location":"api/rows/#CausalFrames.Acausal.lead","page":"Row transformations","text":"lead(offset) -> (CausalPipeline -> CausalPipeline)\nlead(p::CausalPipeline, offset) -> CausalPipeline\n\nA transform shifting every row offset earlier in time (time -> time - offset), so that the value observed at time t is the one the input carried at t + offset — the leading, forward-looking view. Only the :time column changes; all other columns pass through unchanged. This is the mirror of the causal lag.\n\nThis operator is acausal: a row emitted at time t carries data from time t + offset > t. That is why it lives in the CausalFrames.Acausal submodule and must be opted into explicitly (using CausalFrames.Acausal); everything the top-level module exports stays causal.\n\noffset must be non-negative (a negative shift is just a lag); an ArgumentError is thrown when the pipeline runs otherwise, and offset == 0 is the identity. The time type must support adding and subtracting the offset (numbers and Dates types do): the upstream pipeline is run over the window [start + offset, stop + offset) so the shifted output covers [start, stop).\n\nThe curried form composes with |>; the uncurried form applies directly, so lead(p, offset) is equivalent to p |> lead(offset).\n\n\n\n\n\n","title":"CausalFrames.Acausal.lead"},{"category":"function","location":"api/rows/#CausalFrames.settime","page":"Row transformations","text":"settime(spec) -> (CausalPipeline -> CausalPipeline)\nsettime(p::CausalPipeline, spec) -> CausalPipeline\n\nA transform recomputing the :time column. spec is either\n\na Symbol — the named column becomes :time, taking over the position it already occupied, and the old :time column disappears (readcsv's time = :name rename, applied mid-stream); or\na per-row function — spec(row) is called for each row, receiving the same map-like row object as addcolumns, and its result overwrites :time in place, keeping that column's position.\n\nEither way the resulting column is converted to the context's time type and the chunk is re-clipped to the half-open interval [start, stop). All other columns pass through unchanged.\n\nThe transform is causal, and enforces it. Three independent checks, each an ArgumentError when the pipeline runs:\n\nevery row's new time is at least its old one (use CausalFrames.Acausal.settime to move rows earlier);\nthe resulting column is non-decreasing within the chunk;\nand it does not step back across a chunk boundary.\n\nNone implies another: a forward-only map can still reorder rows, and an ordered map can move every row back to start.\n\nThe context is not widened. lag slides its upstream window because its shift is a constant known before any data is read; settime's is per-row and data-dependent, so upstream still runs over [start, stop) and only rows already in the window can be retimed — a row before start that spec would move into the window is never seen. For the same reason settime does not have the chunk-concatenation property: loading [a, c) is not the concatenation of loading [a, b) and [b, c), since a row retimed across b is dropped by the first half and never offered to the second. Streaming still equals loading.\n\nsettime(:time) is legal and leaves the values alone, but it still re-clips to [start, stop) — a row sitting exactly at stop, which frames tolerate and summarize emits, is dropped.\n\nThe curried form composes with |>; the uncurried form applies directly, so settime(p, spec) is equivalent to p |> settime(spec).\n\n\n\n\n\n","title":"CausalFrames.settime"},{"category":"function","location":"api/rows/#CausalFrames.Acausal.settime","page":"Row transformations","text":"CausalFrames.Acausal.settime(spec) -> (CausalPipeline -> CausalPipeline)\nCausalFrames.Acausal.settime(p::CausalPipeline, spec) -> CausalPipeline\n\nThe permissive mirror of settime: the same Symbol-or-function spec, the same conversion to the context's time type, and the same clip — but without the requirement that a row's new time be at least its old one, so rows may move earlier. The result must still be non-decreasing within each chunk and across chunk boundaries, since the chunk protocol depends on it, and it is clipped to [start, stop) at both ends.\n\nThis operator is acausal: a row emitted at time t may carry data the input held at some later time. That is why it lives in the CausalFrames.Acausal submodule; everything the top-level module exports stays causal.\n\nUnlike futurejoin and lead, it is deliberately not exported even from this submodule, so that using CausalFrames.Acausal alongside using CausalFrames leaves the causal settime unambiguous. Reach it as CausalFrames.Acausal.settime.\n\nAs for the causal variant the context is not widened, which bites harder here: a row whose original time is at or past stop is never produced upstream, so a spec that would move it back into the window can never see it — the difference from lead, whose constant shift lets it slide the upstream window instead.\n\nThe curried form composes with |>; the uncurried form applies directly, so settime(p, spec) is equivalent to p |> settime(spec).\n\n\n\n\n\n","title":"CausalFrames.Acausal.settime"},{"category":"section","location":"api/models/#Model-fitting-(MLJ)","page":"Model fitting (MLJ)","text":"With an MLJ model package loaded (using MLJ), FitModel fits a model inside any summarization transform, emitting FittedModels; these operators apply such models to a stream and read their diagnostics.","title":"Model fitting (MLJ)"},{"category":"section","location":"api/models/#applymodels","page":"Model fitting (MLJ)","text":"","title":"applymodels"},{"category":"section","location":"api/models/#addpredictions","page":"Model fitting (MLJ)","text":"","title":"addpredictions"},{"category":"section","location":"api/models/#modelreports","page":"Model fitting (MLJ)","text":"","title":"modelreports"},{"category":"section","location":"api/models/#Fitted-models","page":"Model fitting (MLJ)","text":"","title":"Fitted models"},{"category":"function","location":"api/models/#CausalFrames.applymodels","page":"Model fitting (MLJ)","text":"applymodels(models::CausalPipeline; column = :model, key = nothing,\n            tolerance = nothing, strict = false, name = :prediction,\n            operation = :predict) -> (CausalPipeline -> CausalPipeline)\napplymodels(p::CausalPipeline, models::CausalPipeline; ...) -> CausalPipeline\n\nA transform appending model predictions. models is a pipeline whose column holds FittedModels — the output of FitModel under any summarization transform, or a file of them read back by readjls. Each row is matched to the most recent model row whose time is not after its own (strict = true: strictly before) — asofjoin's rule, including its key and tolerance — and that model's prediction from the row's predictor columns is appended as the column name, of element type Union{Missing, T}. It is missing where no model row qualifies or the matched cell is itself missing (an empty window's summary). A models stream with no rows at all appends an all-missing column.\n\nRows sharing a model are predicted together, in one call per distinct model per chunk. operation chooses the MLJ operation: :predict (the default; a probabilistic model then yields distributions), :predict_mean, :predict_mode or :predict_median. The prediction must be a vector — a multi-target model's table is an ArgumentError.\n\nWith key the models pipeline must carry the key columns and each row uses its own key's latest model. tolerance widens the models' context to [start - tolerance, stop), which is how models fit in an earlier window are applied to a later one; see the manual's Recipes page. Needs MLJModelInterface loaded, as FitModel does.\n\nThe curried form composes with |>; the uncurried form applies directly, so applymodels(p, models; ...) is equivalent to p |> applymodels(models; ...).\n\n\n\n\n\n","title":"CausalFrames.applymodels"},{"category":"function","location":"api/models/#CausalFrames.addpredictions","page":"Model fitting (MLJ)","text":"addpredictions(clock, lookback, model, predictors, response; key = nothing,\n               name = :prediction, operation = :predict,\n               verbosity = 0) -> (CausalPipeline -> CausalPipeline)\naddpredictions(p::CausalPipeline, clock, lookback, model, predictors,\n               response; ...) -> CausalPipeline\n\nA transform appending the predictions of an MLJ model refit on a rolling window. At every tick τ of the clock pipeline the model is fit, regressing response on predictors, over the rows with time in [τ - lookback, τ) — one model per key present there when key is given — and each row at time t is then predicted by the model from the latest tick τ <= t. Every row a model was trained on is therefore strictly earlier than every row it predicts.\n\nThis is summarizewindows over a FitModel feeding applymodels, and it inherits their semantics: rows before the first tick, a key with no rows in the latest window, and a window with no rows all get missing; name and operation are applymodels'; verbosity is passed to the model's fit. The pipeline runs twice, once to fit and once to predict, as a self-join does.\n\nCausality needs one thing from the data: response must be observable at its row's time. A response built by looking ahead (with CausalFrames.Acausal.lead, say) trains every model on values that were not yet known at its tick.\n\n\n\n\n\n","title":"CausalFrames.addpredictions"},{"category":"function","location":"api/models/#CausalFrames.modelreports","page":"Model fitting (MLJ)","text":"modelreports(; column = :model, name = :report) -> (CausalPipeline -> CausalPipeline)\nmodelreports(p::CausalPipeline; column = :model, name = :report) -> CausalPipeline\n\nA row-wise transform over a pipeline of models, replacing its column of FittedModels with a column name holding each model's fit report — exactly what MLJ's report(mach) returns for a machine just after fit!, so a model with nothing to report gives nothing. A missing cell stays missing. The time column, keys and every other column pass through, so the result is a pipeline of diagnostics per tick (and key).\n\nThe report stays one column rather than being spread into its fields: a chunk's columns must be fixed before its rows go out, and a report's fields are unknown until a model has been fit — a stream opening on empty windows would have nothing to name them from. Extract fields with addcolumns, e.g. addcolumns(r -> (; n = r.report.n)).\n\nThe curried form composes with |>; the uncurried form applies directly, so modelreports(p; ...) is equivalent to p |> modelreports(; ...).\n\n\n\n\n\n","title":"CausalFrames.modelreports"},{"category":"type","location":"api/models/#CausalFrames.FittedModel","page":"Model fitting (MLJ)","text":"FittedModel{P,M}\n\nA fitted MLJ model, as FitModel emits it: the model (its hyperparameters, of type M), the fitresult its fit returned — everything prediction needs — and the fit's report of diagnostics (see modelreports). P is the tuple of predictor column names it was fit on, which is how applymodels finds the columns to predict from, so a table of fitted models is self-describing. Serializing one, as writejls does, routes the fitresult through MLJ's save/restore, so models wrapping foreign resources survive the round trip.\n\n\n\n\n\n","title":"CausalFrames.FittedModel"},{"category":"section","location":"api/summarizers/#Summarizers","page":"Summarizers","text":"The values the summarizing transforms fold over their rows. An output column takes its element type from the input column, and is named by suffix — Sum(:mid) produces :mid_sum.","title":"Summarizers"},{"category":"section","location":"api/summarizers/#Count","page":"Summarizers","text":"","title":"Count"},{"category":"section","location":"api/summarizers/#CountDistinct","page":"Summarizers","text":"","title":"CountDistinct"},{"category":"section","location":"api/summarizers/#Sum","page":"Summarizers","text":"","title":"Sum"},{"category":"section","location":"api/summarizers/#SumPower","page":"Summarizers","text":"","title":"SumPower"},{"category":"section","location":"api/summarizers/#Moment","page":"Summarizers","text":"","title":"Moment"},{"category":"section","location":"api/summarizers/#Product","page":"Summarizers","text":"","title":"Product"},{"category":"section","location":"api/summarizers/#DotProduct","page":"Summarizers","text":"","title":"DotProduct"},{"category":"section","location":"api/summarizers/#Mean","page":"Summarizers","text":"","title":"Mean"},{"category":"section","location":"api/summarizers/#Variance","page":"Summarizers","text":"","title":"Variance"},{"category":"section","location":"api/summarizers/#Std","page":"Summarizers","text":"","title":"Std"},{"category":"section","location":"api/summarizers/#Covariance","page":"Summarizers","text":"","title":"Covariance"},{"category":"section","location":"api/summarizers/#Correlation","page":"Summarizers","text":"","title":"Correlation"},{"category":"section","location":"api/summarizers/#LinearRegression","page":"Summarizers","text":"","title":"LinearRegression"},{"category":"section","location":"api/summarizers/#Min","page":"Summarizers","text":"","title":"Min"},{"category":"section","location":"api/summarizers/#Max","page":"Summarizers","text":"","title":"Max"},{"category":"section","location":"api/summarizers/#First","page":"Summarizers","text":"","title":"First"},{"category":"section","location":"api/summarizers/#Last","page":"Summarizers","text":"","title":"Last"},{"category":"section","location":"api/summarizers/#FitModel","page":"Summarizers","text":"","title":"FitModel"},{"category":"section","location":"api/summarizers/#Summarizer-interface","page":"Summarizers","text":"Extend these (unexported — CausalFrames.fresh etc.) to define a custom summarizer.","title":"Summarizer interface"},{"category":"type","location":"api/summarizers/#CausalFrames.Count","page":"Summarizers","text":"Count() -> Summarizer\n\nCounts rows. Produces the output column :count, of type Int.\n\n\n\n\n\n","title":"CausalFrames.Count"},{"category":"type","location":"api/summarizers/#CausalFrames.CountDistinct","page":"Summarizers","text":"CountDistinct(column) -> Summarizer\n\nCounts the distinct values of column. Produces the output column Symbol(column, :_countdistinct), e.g. CountDistinct(:x) produces :x_countdistinct, of type Int. The distinct count of no rows is 0.\n\nmissing counts as a value like any other, so a column holding 1, missing, 1 has two distinct values, and the output column is Int rather than Union{Missing, Int}. This is the one place the accumulating summarizers' poisoning rule does not apply, and deliberately: a sum with a missing term is unknowable, while a distinct count never is — you know exactly how many distinct things you saw. SQL's count(DISTINCT x) skips nulls instead; filterrows(r -> !ismissing(r.x)) upstream recovers that reading.\n\nUnlike every other summarizer, whose state is O(1), this one holds the distinct values it has seen: folding n rows costs O(distinct) memory, and a rolling window pays that per window per key.\n\n\n\n\n\n","title":"CausalFrames.CountDistinct"},{"category":"type","location":"api/summarizers/#CausalFrames.Sum","page":"Summarizers","text":"Sum(column) -> Summarizer\n\nSums column. Produces the output column Symbol(column, :_sum), e.g. Sum(:x) produces :x_sum. The sum of no rows is 0.\n\nThe output column's element type is the one Base.sum would produce: small signed and unsigned integers widen (Int32 sums to Int64), everything else keeps its type (Float32 sums to Float32).\n\nFloating-point accumulators use compensated (Neumaier) summation and count NaN and ±Inf inputs separately: results match Base.sum, and a rolling window recovers exactly once a nonfinite row leaves the window.\n\n\n\n\n\n","title":"CausalFrames.Sum"},{"category":"type","location":"api/summarizers/#CausalFrames.SumPower","page":"Summarizers","text":"SumPower(column, n) -> Summarizer\n\nSums column raised to the power n. Produces the output column Symbol(column, :_sumpower_, n), e.g. SumPower(:x, 2) produces :x_sumpower_2. The sum of no rows is 0. The output column's element type follows the same rule as Sum, applied to the type of column ^ n. Each term is formed in the accumulator's (widened) type before raising to the power, so a per-row power cannot overflow the way raising in the input column's own type would. Floating-point accumulators use compensated summation with NaN and ±Inf terms (the value after raising to the power) counted separately, as in Sum.\n\nSumPower(column, 1) produces :x_sumpower_1, a distinct column from Sum(:x)'s :x_sum.\n\n\n\n\n\n","title":"CausalFrames.SumPower"},{"category":"type","location":"api/summarizers/#CausalFrames.Moment","page":"Summarizers","text":"Moment(column, n) -> Summarizer\n\nThe n-th raw moment of column: the mean of column ^ n. Produces the output column Symbol(column, :_moment_, n), e.g. Moment(:x, 2) produces :x_moment_2; Moment(:x, 1) is the mean. The moment of no rows is missing.\n\nA dependent summarizer, computed as SumPower(column, n) divided by Count — those are folded alongside it but appear in the output only if requested themselves. The output column's element type is the division's result (Int input divides to Float64, Float32 stays Float32).\n\n\n\n\n\n","title":"CausalFrames.Moment"},{"category":"type","location":"api/summarizers/#CausalFrames.Product","page":"Summarizers","text":"Product(column) -> Summarizer\n\nMultiplies column. Produces the output column Symbol(column, :_product), e.g. Product(:x) produces :x_product. The product of no rows is 1.\n\nThe output column's element type is the one Base.prod would produce: small signed and unsigned integers widen (Int32 multiplies to Int64), everything else keeps its type (Float32 stays Float32). Like Sum, the accumulator is built at that width up front.\n\n\n\n\n\n","title":"CausalFrames.Product"},{"category":"type","location":"api/summarizers/#CausalFrames.DotProduct","page":"Summarizers","text":"DotProduct(a, b) -> Summarizer\n\nSums the elementwise product of columns a and b. Produces the output column Symbol(a, :_, b, :_dotproduct), e.g. DotProduct(:x, :y) produces :x_y_dotproduct. The dot product of no rows is 0.\n\nThe output column's element type is Base.sum applied to the type of a * b, so it widens the same way Sum does. Each term is formed in the accumulator's (widened) type, so a per-row product cannot overflow the way multiplying in the input columns' own types would. Floating-point accumulators use compensated summation with NaN and ±Inf terms (the per-row product, so Inf * 0.0 counts as a NaN term) counted separately, as in Sum.\n\nThe value is symmetric, so the fold happens under one canonical (sorted) argument order: DotProduct(:y, :x) still produces :y_x_dotproduct, but it folds nothing of its own — it is a dependent summarizer over DotProduct(:x, :y), renaming that value. Asking both ways in one call, or asking one way beside a Covariance or LinearRegression that needs the same product, therefore costs one accumulator rather than two.\n\n\n\n\n\n","title":"CausalFrames.DotProduct"},{"category":"type","location":"api/summarizers/#CausalFrames.Mean","page":"Summarizers","text":"Mean(column) -> Summarizer\n\nThe mean of column. Produces the output column Symbol(column, :_mean), e.g. Mean(:x) produces :x_mean. The mean of no rows is missing.\n\nA dependent summarizer, computed as Sum(column) divided by Count — those are folded alongside it but appear in the output only if requested themselves. The output column's element type is the division's result (Int input divides to Float64, Float32 stays Float32).\n\n\n\n\n\n","title":"CausalFrames.Mean"},{"category":"type","location":"api/summarizers/#CausalFrames.Variance","page":"Summarizers","text":"Variance(column; corrected = true) -> Summarizer\n\nThe variance of column, following Statistics.var: divided by n - 1 when corrected (the default), by n otherwise. Produces the output column Symbol(column, :_variance), e.g. Variance(:x) produces :x_variance. The variance of no rows is missing; the corrected variance of a single row is NaN (0.0 when corrected = false).\n\nA dependent summarizer, computed from Count, Sum(column), and SumPower(column, 2) by the identity (Σx² − (Σx)²/n) / (n − corrected); those are folded alongside it but appear in the output only if requested themselves. The output column's element type is the computation's result (Float64 for integer input, Float32 for Float32).\n\ncorrected is not part of the output name, so a corrected and an uncorrected Variance of the same column cannot be requested together in one call — they would share :x_variance and collapse under the name-keyed deduplication.\n\n\n\n\n\n","title":"CausalFrames.Variance"},{"category":"type","location":"api/summarizers/#CausalFrames.Std","page":"Summarizers","text":"Std(column; corrected = true) -> Summarizer\n\nThe standard deviation of column, following Statistics.std: the square root of Variance(column; corrected). Produces the output column Symbol(column, :_std), e.g. Std(:x) produces :x_std. The standard deviation of no rows is missing, and of a single corrected row is NaN.\n\nA dependent summarizer that folds Variance(column; corrected) alongside it (which appears in the output only if requested itself). A round-off-negative variance is clamped to zero before the square root, so folding never raises a DomainError.\n\n\n\n\n\n","title":"CausalFrames.Std"},{"category":"type","location":"api/summarizers/#CausalFrames.Covariance","page":"Summarizers","text":"Covariance(a, b; corrected = true) -> Summarizer\n\nThe covariance of columns a and b, following Statistics.cov: divided by n - 1 when corrected (the default), by n otherwise. Produces the output column Symbol(a, :_, b, :_covariance), e.g. Covariance(:x, :y) produces :x_y_covariance. The covariance of no rows is missing; the corrected covariance of a single row is NaN.\n\nA dependent summarizer, computed from Count, Sum(a), Sum(b), and DotProduct(a, b) by the identity (Σ(ab) − ΣaΣb/n) / (n − corrected); those are folded alongside it but appear in the output only if requested themselves. Covariance(:x, :x; corrected) equals Variance(:x; corrected).\n\nLike Variance, corrected is not part of the output name.\n\n\n\n\n\n","title":"CausalFrames.Covariance"},{"category":"type","location":"api/summarizers/#CausalFrames.Correlation","page":"Summarizers","text":"Correlation(a, b) -> Summarizer\n\nThe Pearson correlation of columns a and b, following Statistics.cor: Covariance(a, b) divided by the product of the two columns' Stds, clamped to [-1, 1]. Produces the output column Symbol(a, :_, b, :_correlation), e.g. Correlation(:x, :y) produces :x_y_correlation. The correlation of no rows is missing, and of a single row is NaN.\n\nUnlike Covariance and Std, Correlation takes no corrected keyword: the n - 1 (or n) factor cancels between the covariance and the standard deviations, so the value is the same either way. It is a dependent summarizer over Covariance(a, b), Std(a), and Std(b); those are folded alongside it but appear in the output only if requested themselves.\n\n\n\n\n\n","title":"CausalFrames.Correlation"},{"category":"type","location":"api/summarizers/#CausalFrames.LinearRegression","page":"Summarizers","text":"LinearRegression(predictors, response; intercept = true, name = nothing)\n\nOrdinary least squares of response on predictors — a collection of column names, or a single column name. With intercept (the default) the fit carries a constant term.\n\nOutput columns\n\nname, when given, prefixes every output column as Symbol(name, :_, base); the base names are:\n\n# base column meaning\n1 n rows folded in\n2 r2 coefficient of determination\n3 stderr residual standard error\n4 intercept_beta the constant term, omitted when intercept = false\n5 intercept_tstat its t statistic, omitted when intercept = false\n6.. Symbol(p, :_beta), Symbol(p, :_tstat) per predictor p, in the order given\n\nso 2K + 5 columns for K predictors with an intercept, 2K + 3 without:\n\nLinearRegression([:x, :z], :y)\n# :n, :r2, :stderr, :intercept_beta, :intercept_tstat,\n# :x_beta, :x_tstat, :z_beta, :z_tstat\n\nLinearRegression([:x, :z], :y; name = :m1)\n# :m1_n, :m1_r2, :m1_stderr, :m1_intercept_beta, :m1_intercept_tstat,\n# :m1_x_beta, :m1_x_tstat, :m1_z_beta, :m1_z_tstat\n\nThe un-prefixed defaults are deliberately grabby: n, r2, and stderr say nothing about the model, so two regressions in one call need distinct names — without them they collide on those three columns even when their predictors differ entirely, and the collision is an error. A regression and the same regression with intercept flipped likewise cannot share a name. A predictor named intercept collides with the constant term's own pair and is rejected by the constructor; the other model-level names take no suffix, so a predictor named n, r2, or stderr is fine.\n\nThe 2K + 4 statistic columns share one element type, the computation's result (Float64 for integer input, Float32 for Float32), and a Missing-admitting input makes all of them Union{Missing,…}. n is separately Int, exactly as Count's :count is, and is never missing.\n\nThe regression of no rows is missing in every statistic and 0 in n; so is any window holding a missing in a predictor or the response. A rank-deficient system — collinear predictors, or n ≤ K (n ≤ K + 1 with an intercept) — gives NaN statistics rather than raising, as Correlation does for a single row. With no residual degrees of freedom left (n = K + 1, or n = K without an intercept) the coefficients and r2 are the exact fit but stderr and every t statistic are NaN, and a constant response makes r2 NaN. n is the honest row count throughout, so a poisoned fit still says how much data it saw.\n\nSharing\n\nA dependent summarizer over Count, SumPower(·, 2), DotProduct, and — only when intercept — Sum: the whole normal-equations system and every statistic drawn from it are functions of those. They are folded alongside the regression but appear in the output only if requested themselves, and because dependencies deduplicate by output name, two regressions over overlapping columns fold each cross product exactly once. Cross products are requested under the canonical (sorted) argument order every symmetric summarizer shares, so a Covariance or DotProduct requested separately shares the accumulator whichever way round it is written. A squared term is requested as SumPower(c, 2) — the name Variance, Std, and Correlation already depend on — so a regression run beside them shares that too.\n\nWith an intercept the system is centered on the column means, the multivariate form of the identity Covariance uses: better conditioned, and one dimension smaller than carrying a column of ones. Without one, r2 is the uncentered coefficient of determination, as is conventional for a no-intercept fit.\n\nUnlike every other dependent summarizer, a multiple regression allocates: K ≥ 2 builds a K × K workspace per emitted row. Simple regression (K = 1, the common case) runs a closed form over scalars and allocates nothing.\n\n\n\n\n\n","title":"CausalFrames.LinearRegression"},{"category":"type","location":"api/summarizers/#CausalFrames.Min","page":"Summarizers","text":"Min(column) -> Summarizer\n\nTracks the minimum of column. Produces the output column Symbol(column, :_min), e.g. Min(:x) produces :x_min, with the same element type as column. The minimum of no rows is missing.\n\n\n\n\n\n","title":"CausalFrames.Min"},{"category":"type","location":"api/summarizers/#CausalFrames.Max","page":"Summarizers","text":"Max(column) -> Summarizer\n\nTracks the maximum of column. Produces the output column Symbol(column, :_max), e.g. Max(:x) produces :x_max, with the same element type as column. The maximum of no rows is missing.\n\n\n\n\n\n","title":"CausalFrames.Max"},{"category":"type","location":"api/summarizers/#CausalFrames.First","page":"Summarizers","text":"First(column) -> Summarizer\n\nKeeps the value of column from the first row folded in. Produces the output column Symbol(column, :_first), e.g. First(:x) produces :x_first, with the same element type as column. The first of no rows is missing.\n\n\n\n\n\n","title":"CausalFrames.First"},{"category":"type","location":"api/summarizers/#CausalFrames.Last","page":"Summarizers","text":"Last(column) -> Summarizer\n\nKeeps the value of column from the most recent row folded in. Produces the output column Symbol(column, :_last), e.g. Last(:x) produces :x_last, with the same element type as column. The last of no rows is missing.\n\n\n\n\n\n","title":"CausalFrames.Last"},{"category":"type","location":"api/summarizers/#CausalFrames.FitModel","page":"Summarizers","text":"FitModel(model, predictors, response; name = :model,\n         verbosity = 0) -> Summarizer\n\nFits an MLJ model — any MLJModelInterface.Model, so anything in MLJ's model registry — regressing response on predictors (a column name or a collection of them) over the rows it folds. Produces one output column, name, holding a FittedModel; the summary of no rows is missing. Needs MLJModelInterface loaded — using MLJ, or any MLJ model package; most model implementations also need MLJBase, which using MLJ loads.\n\nThe folded rows are buffered and the model is fit when a summary is emitted, so a fit's cost follows the host transform: once per window under summarize, per interval under intervalize, per tick (and key) under summarizewindows — the natural hosts. Under addsummarycolumns it refits after every row, over everything seen so far, which is quadratic. verbosity is passed to the model's fit.\n\nThe predictors reach the model as a column table of the input's own element types, with no scientific-type coercion — coerce upstream with addcolumns where a model needs it — and missing values are passed through as they are. The model is handed its own copy of the rows, so a fitted model that keeps its training data is never disturbed by later fits.\n\nA fit neither combines nor inverts, so FitModel is a plain Summarizer: windowed transforms re-fold each window for it. addpredictions fits and applies models over a rolling window in one step.\n\nMLJ exports the scientific type Count, so with both using MLJ and using CausalFrames the Count summarizer must be written CausalFrames.Count().\n\n\n\n\n\n","title":"CausalFrames.FitModel"},{"category":"type","location":"api/summarizers/#CausalFrames.Summarizer","page":"Summarizers","text":"Summarizer\n\nAbstract supertype for summarization configurations. A concrete summarizer is immutable and holds only configuration — typically the column to summarize, carried as a type parameter so that the output column names it implies are known to the compiler. It implements:\n\nemptyvalue(s) — the summary of no rows, as a NamedTuple whose keys are the output column names;\nfresh(s, intypes) — a zero SummarizerState, typed for input columns whose element types are given by intypes.\n\nOutput column names are deterministic, formed by suffixing the column name (e.g. Sum(:x) produces :x_sum); summarizers with identical output names are treated as identical and share state.\n\nA summarizer may depend on the values of other summarizers by implementing dependencies(s) and the two-argument form of value; dependencies are resolved through the same name-keyed deduplication, so shared work is computed once, and appear in the output only when requested by the user themselves.\n\nStructured subtypes refine what the state supports: a MonoidSummarizer's states combine associatively (combine!), and a GroupSummarizer's updates are additionally invertible (downdate!). addrollingcolumns selects faster window algorithms when every summarizer it folds declares the structure.\n\n\n\n\n\n","title":"CausalFrames.Summarizer"},{"category":"type","location":"api/summarizers/#CausalFrames.MonoidSummarizer","page":"Summarizers","text":"MonoidSummarizer <: Summarizer\n\nA summarizer whose states form a monoid: combine! merges the states of two adjacent, stream-ordered row ranges into the state of their concatenation, associatively, with a fresh state as the identity. addrollingcolumns exploits this to answer each window from a tree of partial combinations — O(log window) per row — instead of re-folding every window row.\n\n\n\n\n\n","title":"CausalFrames.MonoidSummarizer"},{"category":"type","location":"api/summarizers/#CausalFrames.GroupSummarizer","page":"Summarizers","text":"GroupSummarizer <: MonoidSummarizer\n\nA monoid summarizer whose updates can also be undone: downdate! removes a previously folded row. addrollingcolumns exploits this to slide each window in O(1) amortized per row, subtracting the exiting rows from a running state — unless isinvertible reports that the realized accumulator type defeats the inverse (an absorbing value folded past recovery), in which case it falls back to the monoid tree.\n\n\n\n\n\n","title":"CausalFrames.GroupSummarizer"},{"category":"type","location":"api/summarizers/#CausalFrames.SummarizerState","page":"Summarizers","text":"SummarizerState\n\nAbstract supertype for the running state of one summarization, built by fresh(s, intypes) from a Summarizer and the input columns' element types. Because the state's value fields are concrete, so are the columns it produces. It implements:\n\nfresh(st) — a new state of the same type with zero state;\nupdate!(st, row) — fold one row into the state;\nvalue(st) — the current summary as a NamedTuple whose keys are the output column names;\nwidenstate(st, intypes) — optionally, a state rebuilt for widened input columns.\n\n\n\n\n\n","title":"CausalFrames.SummarizerState"},{"category":"function","location":"api/summarizers/#CausalFrames.emptyvalue","page":"Summarizers","text":"emptyvalue(s::Summarizer) -> NamedTuple\n\nThe summary of no rows, keyed by output column name. This is the only summary available when the input has no rows at all: the chunk protocol never yields an empty chunk, so an empty input carries no schema and no state can be built for it. It is also where the summarization transforms read a summarizer's output column names, before any data has been seen.\n\n\n\n\n\n","title":"CausalFrames.emptyvalue"},{"category":"function","location":"api/summarizers/#CausalFrames.fresh","page":"Summarizers","text":"fresh(s::Summarizer, intypes::NamedTuple) -> SummarizerState\nfresh(st::SummarizerState) -> SummarizerState\n\nA zero state. The first form builds one from a summarizer and the input columns' element types, mirroring the row access in update!: update! reads row[column] where fresh reads intypes[column]. The second form produces a new state of the same concrete type as st, which is how the transforms obtain per-key-group and per-cycle states without re-consulting the schema.\n\nThe summarization transforms treat the summarizers they are given as prototypes, so one prototype serves many key groups and the caller's instance is never mutated.\n\n\n\n\n\n","title":"CausalFrames.fresh"},{"category":"function","location":"api/summarizers/#CausalFrames.fresh!","page":"Summarizers","text":"fresh!(st::SummarizerState) -> SummarizerState\n\nZero st in place and return it — the in-place counterpart of the one-argument fresh. Callers must use the returned value rather than assume st was mutated: the default implementation is fresh(st), so a state that cannot be zeroed in place (an immutable one) simply returns a new object, and every existing summarizer keeps working without implementing this at all.\n\nImplementing it is a pure optimization, and worth it for any state on a hot path: the transforms zero a state tuple per cycle (summarizecycles), per window query (the addrollingcolumns tree mode), and per window per row (its re-fold mode), so an allocating fresh there costs one heap allocation per state per row.\n\nThe zeroed state must be indistinguishable from fresh(st) through the rest of the interface. The one exception is the same one fresh already carries: a state whose value field is only meaningful once a row has been folded in (Min/Max/First/Last) may leave a stale value behind, because value is never called on a state that has folded no rows.\n\n\n\n\n\n","title":"CausalFrames.fresh!"},{"category":"function","location":"api/summarizers/#CausalFrames.update!","page":"Summarizers","text":"update!(st::SummarizerState, row)\n\nFold one row into the state. row is a map-like row object supporting row.name and row[:name] access, including row.time, so a summarizer may read whichever columns it needs.\n\n\n\n\n\n","title":"CausalFrames.update!"},{"category":"function","location":"api/summarizers/#CausalFrames.value","page":"Summarizers","text":"value(st::SummarizerState) -> NamedTuple\nvalue(st::SummarizerState, vals::NamedTuple) -> NamedTuple\n\nThe current summary. A summarizer may produce several values; the keys of the returned NamedTuple are the output column names, and its value types are the element types of the columns produced.\n\nThe two-argument form receives in vals the already-computed values of every summarizer earlier in topological order — in particular of everything named by dependencies. It defaults to calling the one-argument form; a dependent summarizer implements the two-argument form instead and may omit the one-argument form entirely.\n\nOnly ever called on a state that has folded at least one row — the summary of no rows is emptyvalue.\n\n\n\n\n\n","title":"CausalFrames.value"},{"category":"function","location":"api/summarizers/#CausalFrames.widenstate","page":"Summarizers","text":"widenstate(st::SummarizerState, intypes::NamedTuple) -> SummarizerState\n\nA state equivalent to st but typed for input columns of the element types in intypes, carrying the accumulated value over. A source may hand a column a different element type from one chunk to the next, so a column can be Int in one chunk and Float64 in the next; the transforms promote the types they have seen and call widenstate when that promotion changes something.\n\nDefaults to returning st unchanged, which is correct for any state whose type does not depend on the input, and which lets a summarizer opt out.\n\n\n\n\n\n","title":"CausalFrames.widenstate"},{"category":"function","location":"api/summarizers/#CausalFrames.dependencies","page":"Summarizers","text":"dependencies(s::Summarizer) -> Tuple\n\nThe summarizers whose values s reads in the two-argument form of value. The summarization transforms expand dependencies — recursively, in topological order — into the set of summarizers they fold, deduplicated by output name, so a dependency equal to a user-requested summarizer is computed once. Dependencies appear in the output only when the user requested them themselves.\n\nDefaults to (), which is correct for any self-contained summarizer.\n\n\n\n\n\n","title":"CausalFrames.dependencies"},{"category":"function","location":"api/summarizers/#CausalFrames.combine!","page":"Summarizers","text":"combine!(dest::SummarizerState, a::SummarizerState, b::SummarizerState)\n\nOverwrite dest with the combination of a and b: the state that folding a's rows and then b's rows into a fresh state would produce. Required of a MonoidSummarizer's states, with the monoid laws: combination is associative, and a fresh state is the identity on either side. Callers guarantee that every row folded into a precedes every row folded into b in stream order — which is what lets order-sensitive summarizers like First and Last combine. All three states are of the same concrete type, and dest may alias a or b, so an implementation reads its inputs before writing.\n\n\n\n\n\n","title":"CausalFrames.combine!"},{"category":"function","location":"api/summarizers/#CausalFrames.downdate!","page":"Summarizers","text":"downdate!(st::SummarizerState, row)\n\nRemove one previously folded row from the state — the inverse of update!. Required of a GroupSummarizer's states. The inverse is exact when the accumulator arithmetic is (integer sums); the floating-point sum accumulators use compensated summation with NaN and ±Inf terms counted separately, so nonfinite rows subtract away exactly and finite ones leave only the small compensated round-off. missing terms are likewise counted rather than folded in (see the Optional* accumulator states), so a missing row also subtracts away exactly — the count balances — leaving the accumulator invertible.\n\n\n\n\n\n","title":"CausalFrames.downdate!"},{"category":"function","location":"api/summarizers/#CausalFrames.isinvertible","page":"Summarizers","text":"isinvertible(st::SummarizerState) -> Bool\n\nWhether downdate! actually inverts update! for this state's realized accumulator type. Defaults to true. The sum-family accumulators keep NaN, ±Inf, and missing terms out of the running total and count them instead, so they stay invertible; a state that folds an absorbing value into an unrecoverable running total returns false. addrollingcolumns consults this when choosing the running-state window algorithm.\n\n\n\n\n\n","title":"CausalFrames.isinvertible"},{"category":"section","location":"recipes/#Recipes","page":"Recipes","text":"Four patterns that are not obvious from the operator list. Each is a way of saying something inside the pipeline that looks at first as though it needs an exit to DataFrame.","title":"Recipes"},{"category":"section","location":"recipes/#Ranking-within-a-group","page":"Recipes","text":"addsummarycolumns holds each summary after the current row has been folded in, so a keyed Count is a 1-based ordinal within its key — the nth row of that key so far:\n\np |> addsummarycolumns(Count(); key = :symbol)   # :count is 1, 2, 3, … per symbol\n\nThat ordinal counts in stream order, which is time order, so what it gives you directly is row_number() OVER (PARTITION BY symbol ORDER BY time). An arbitrary ORDER BY is available wherever the row order within a group is free — which is exactly when the partition column is :time itself, since rows sharing a timestamp may arrive in any order. Sorting them at the source turns the same count into a rank by whatever you sorted on:\n\n# rows arrive sorted (year, votes descending), and :time is the year\nreadparquet(\"films.parquet\"; time = :year) |>\n    addsummarycolumns(Count(); key = :time) |>\n    filterrows(r -> r.count <= 250)       # the 250 most-voted films of each year\n\nThe ORDER BY half is always the source's job. CausalFrames has no sort operator, deliberately: monotone time is the invariant that makes streaming sound, and a sort is not streaming. So the rank is only ever as meaningful as the order the rows arrived in — which is worth asserting on if the source is not under your control.","title":"Ranking within a group"},{"category":"section","location":"recipes/#Joining-a-reference-table","page":"Recipes","text":"asofjoin matches the most recent right row at or before each left row's time. Give every right row the same time and that degenerates into a plain keyed lookup: there is only one row per key, and it is always at or before whatever the left row's time is. A dimension table, joined by key alone:\n\ndim = readcsv(\"dim.csv\"; types = Dict(:id => String), time = _ -> WINDOW_START)\nfacts |> asofjoin(dim; key = :id)\n\nMemory is O(keys), as for any asofjoin.\n\nThe constant must fall inside the evaluation window. Sources clip to [start, stop), so a right pipeline timed outside it yields no rows at all — and a right stream producing no chunks passes the left chunks through unchanged, meaning the right columns are absent from the schema rather than present and missing. The symptom is a confusing \"column not found\" from whatever reads them next, at some distance from the cause. Time the reference rows at the window's own start.","title":"Joining a reference table"},{"category":"section","location":"recipes/#Using-a-derived-stream-as-a-reference-table","page":"Recipes","text":"The recipe above needs constant-timed rows, and a stream that already carries real times cannot be retimed backward: settime may only move rows later. So to look up against something the pipeline itself derived, materialize it and read it back at a constant time. writecsv is a pass-through sink built for exactly this, and scan drives the pipeline for the side effect alone:\n\nderived |> selectcolumns(:id) |> writecsv(path) |> scan(ctx)\n\nlookup = readcsv(path; types = Dict(:id => String), time = _ -> WINDOW_START)\nfacts |> asofjoin(lookup; key = :id)\n\nNote that this is not a way around a pipeline being single-pass — it is not. A CausalPipeline is a lazy description of how to produce data, and load, stream and scan may each run it again; only the iterator produced by one such run is single-pass. Materializing here buys the retiming, not the second read.\n\nIf you would rather move the rows than write them out, CausalFrames.Acausal.settime is the permissive form that may move rows earlier. It lives in the Acausal submodule because that is a genuine loss of causality, and it must be opted into by name.","title":"Using a derived stream as a reference table"},{"category":"section","location":"recipes/#Fitting-a-model-once-and-applying-it-later","page":"Recipes","text":"addpredictions refits as the stream moves. To fit one model over a training window and apply it to a later one, fit it with summarize, persist the one-row model table with writejls — CSV and parquet cannot hold a fitted model — and read it back as the models pipeline of applymodels:\n\nusing MLJ                                   # loads the MLJ integration\nmodel = (@load LinearRegressor pkg = MLJLinearModels)()\n\ntrain = Context(DateTime(2024, 1, 1), DateTime(2025, 1, 1))\nticks |> summarize(FitModel(model, [:mid, :size], :ret)) |>\n    writejls(\"model.jls\") |> scan(train)\n\nlater = Context(DateTime(2026, 1, 1), DateTime(2027, 1, 1))\nticks |> applymodels(readjls(\"model.jls\"); tolerance = Year(3)) |> load(later)\n\nThis is the reference-table problem again. summarize emits its row at the training window's stop, 2025-01-01, and readjls clips to the window it runs over like every source, so over later alone the model row is clipped away and every prediction is missing. tolerance widens the models' window backward far enough to reach it — and, being an as-of tolerance, also bounds how old a model may be for each row. (Applied over a window that starts at the training stop, the row is inside it and no tolerance is needed.)\n\nThe same file gives the fit's diagnostics: readjls(\"model.jls\") |> modelreports() over any window containing the model's time.","title":"Fitting a model once and applying it later"},{"category":"section","location":"api/sources/#Sources","page":"Sources","text":"A source starts a pipeline: it produces rows rather than transforming them, and clips what it produces to the context's half-open interval [start, stop). These construct or combine streams without touching a file — the file-backed sources are under File I/O.","title":"Sources"},{"category":"section","location":"api/sources/#emptyframe","page":"Sources","text":"","title":"emptyframe"},{"category":"section","location":"api/sources/#concatenate","page":"Sources","text":"","title":"concatenate"},{"category":"section","location":"api/sources/#merge","page":"Sources","text":"","title":"merge"},{"category":"section","location":"api/sources/#clock","page":"Sources","text":"","title":"clock"},{"category":"function","location":"api/sources/#CausalFrames.emptyframe","page":"Sources","text":"emptyframe() -> CausalPipeline\n\nA source that always produces zero rows (loading it yields a frame with only a :time column).\n\n\n\n\n\n","title":"CausalFrames.emptyframe"},{"category":"function","location":"api/sources/#CausalFrames.concatenate","page":"Sources","text":"concatenate(ps::CausalPipeline...) -> CausalPipeline\n\nA source running the given pipelines one after another over the same context and emitting their chunks end to end — the time-wise concatenation of their outputs. There is no interleaving and no merging: the pipelines must be passed in time order and must produce identical column names.\n\nEvery pipeline is evaluated over the whole context [start, stop) and clips itself, so keeping their windows from overlapping is the caller's business. Each is run only once the previous one is exhausted, which keeps the chain as lazy as its parts — a chain of file sources holds one file open at a time.\n\nBoth requirements are checked as the chunks flow by: an ArgumentError is thrown when a chunk's column names differ from the first chunk's, or when a chunk's first time precedes the last time already emitted (equal times across a boundary are fine). Element types may differ between pipelines, as they may between the chunks of one pipeline — DataFrame(frame) promotes on concatenation.\n\nconcatenate() with no pipelines is emptyframe, the identity of concatenation.\n\nconcatenate(readcsv(\"jan.csv\"; types = tt), readcsv(\"feb.csv\"; types = tt)) |>\n    filterrows(r -> r.bid > 0)\n\n\n\n\n\n","title":"CausalFrames.concatenate"},{"category":"method","location":"api/sources/#Base.merge-Tuple{CausalPipeline, Vararg{CausalPipeline}}","page":"Sources","text":"merge(p::CausalPipeline, ps::CausalPipeline...; batchsize = 1024) -> CausalPipeline\n\nA source running the given pipelines concurrently over the same context and interleaving their rows by time — the time-wise merge of their outputs, as opposed to concatenate's end-to-end join.\n\nThe pipelines need not have the same columns. The output carries the union of their columns, :time first and the rest in the order the pipelines introduce them; a row coming from a pipeline that lacks a column carries missing there, so a merged column of T has element type Union{Missing, T} once the frame is materialized. Element types may differ between pipelines, as they may between the chunks of one pipeline — DataFrame(frame) promotes on concatenation.\n\nRows at equal times are emitted in argument order: all the tied rows of the first pipeline, then those of the second, and so on. Each pipeline's own row order is preserved. Every pipeline is evaluated over the whole context [start, stop) and clips itself; unlike concatenate, they run at the same time, so a merge of file sources holds every file open at once and buffers one chunk per input.\n\nA pipeline's column names are fixed by its first chunk: a later chunk whose names differ, in content or in order, is an ArgumentError naming the pipeline. A pipeline that produces no chunk at all over the window contributes no columns, exactly as an empty right stream contributes none to asofjoin. Rows are emitted in batches of at least batchsize (the trailing one excepted), so streams that alternate row by row still yield chunk-sized output rather than a chunk per row.\n\nThere is no zero-argument form: emptyframe is the identity of merging.\n\nmerge(readcsv(\"trades.csv\"; types = tt), readcsv(\"quotes.csv\"; types = qt)) |>\n    filterrows(r -> ismissing(r.bid) || r.bid > 0)\n\n\n\n\n\n","title":"Base.merge"},{"category":"function","location":"api/sources/#CausalFrames.clock","page":"Sources","text":"clock(interval; batchsize = 1024) -> CausalPipeline\n\nA source producing one row per interval at times start, start + interval, ... while < stop, with no columns other than :time. interval may be anything addable to the context's time type (e.g. a Dates.Period for DateTime, a number for numeric time). Ticks are generated lazily in chunks of batchsize rows.\n\n\n\n\n\n","title":"CausalFrames.clock"},{"category":"section","location":"api/columns/#Column-transformations","page":"Column transformations","text":"The column set itself: which columns exist and in what order, and filling or joining values into them. Rows pass through one for one — no row is added, dropped, or retimed here.\n\nThe forward-looking join lives in the CausalFrames.Acausal submodule and is never re-exported — reach it with using CausalFrames.Acausal.","title":"Column transformations"},{"category":"section","location":"api/columns/#selectcolumns","page":"Column transformations","text":"","title":"selectcolumns"},{"category":"section","location":"api/columns/#dropcolumns","page":"Column transformations","text":"","title":"dropcolumns"},{"category":"section","location":"api/columns/#reordercolumns","page":"Column transformations","text":"Two ways to resolve missing values — the ones a merge schema union, an asofjoin non-match or a nullable parquet column leaves behind. Only fillmissing is row-wise; forwardfill carries the last non-missing value of every filled column across rows, chunks and keys.","title":"reordercolumns"},{"category":"section","location":"api/columns/#forwardfill","page":"Column transformations","text":"","title":"forwardfill"},{"category":"section","location":"api/columns/#fillmissing","page":"Column transformations","text":"A join appends another pipeline's columns alongside each row, matching by time.","title":"fillmissing"},{"category":"section","location":"api/columns/#asofjoin","page":"Column transformations","text":"","title":"asofjoin"},{"category":"section","location":"api/columns/#Acausal.futurejoin","page":"Column transformations","text":"","title":"Acausal.futurejoin"},{"category":"function","location":"api/columns/#CausalFrames.selectcolumns","page":"Column transformations","text":"selectcolumns(selectors...) -> (CausalPipeline -> CausalPipeline)\nselectcolumns(p::CausalPipeline, selectors...) -> CausalPipeline\n\nA transform keeping only the selected columns, in the input's own column order. Each selector is a column name (a Symbol or AbstractString), a Regex matched against the column name, a predicate called with the column name as a String, or — recursively — any collection of those; a column is kept when it matches any of them.\n\n:time is always kept, whether or not it is selected. Naming a column that the data does not have is an ArgumentError; a Regex or predicate matching nothing is not.\n\nThe curried form composes with |>; the uncurried form applies directly, so selectcolumns(p, sel) is equivalent to p |> selectcolumns(sel).\n\np |> selectcolumns(:bid, :ask)\np |> selectcolumns(r\"^px_\", startswith(\"qty\"))\n\n\n\n\n\n","title":"CausalFrames.selectcolumns"},{"category":"function","location":"api/columns/#CausalFrames.dropcolumns","page":"Column transformations","text":"dropcolumns(selectors...) -> (CausalPipeline -> CausalPipeline)\ndropcolumns(p::CausalPipeline, selectors...) -> CausalPipeline\n\nA transform dropping the selected columns and keeping the rest, in the input's own column order. Selectors take the same forms as for selectcolumns, and a column is dropped when it matches any of them.\n\n:time is never dropped — a Regex or predicate matching it is ignored, and naming it outright is an ArgumentError, since every frame must have a time column. Naming a column that the data does not have is an ArgumentError too.\n\nThe curried form composes with |>; the uncurried form applies directly, so dropcolumns(p, sel) is equivalent to p |> dropcolumns(sel).\n\n\n\n\n\n","title":"CausalFrames.dropcolumns"},{"category":"function","location":"api/columns/#CausalFrames.reordercolumns","page":"Column transformations","text":"reordercolumns(selectors...) -> (CausalPipeline -> CausalPipeline)\nreordercolumns(p::CausalPipeline, selectors...) -> CausalPipeline\n\nA transform moving the selected columns to the front, in the selectors' order, and leaving the rest behind them in the input's own column order. Selectors take the same forms as for selectcolumns, except that here their order is what the output order follows: nested collections are flattened in place, so reordercolumns([:a, :b]) orders as reordercolumns(:a, :b) does. A Regex or predicate matching several columns contributes them in the input's order, and a column matched by more than one selector is placed by the first of them.\n\nNaming only some of the columns is the point — the rest keep their relative order at the back, so a reorder never has to re-list the whole schema.\n\n:time is always first, whatever the selectors say: a Regex or predicate matching it is ignored, and naming it outright is an ArgumentError. Naming a column that the data does not have is an ArgumentError too; a Regex or predicate matching nothing is not.\n\nThe curried form composes with |>; the uncurried form applies directly, so reordercolumns(p, sel) is equivalent to p |> reordercolumns(sel).\n\np |> reordercolumns(:px, :size)   # time, px, size, then the rest\np |> reordercolumns(r\"^px_\")\n\n\n\n\n\n","title":"CausalFrames.reordercolumns"},{"category":"function","location":"api/columns/#CausalFrames.forwardfill","page":"Column transformations","text":"forwardfill(selectors...; key = nothing, tolerance = nothing)\n    -> (CausalPipeline -> CausalPipeline)\nforwardfill(p::CausalPipeline, selectors...; key = nothing,\n            tolerance = nothing) -> CausalPipeline\n\nA transform replacing missing in the selected columns with that column's last non-missing value. Every row and every other column is kept; a filled column keeps element type Union{Missing, T}, since rows before the column's first non-missing value — and rows past tolerance — stay missing.\n\nThe columns are named by the same selector forms as selectcolumns: a name, a Regex, a name predicate, or a collection of those. :time and the key columns are never filled, even when a selector matches them; a selector naming a column the data does not have is an ArgumentError. Each column is filled independently, so one column may carry a value from a much earlier row than another.\n\nWith key (a column name or collection of column names) the last non-missing value is tracked per distinct key value, so one key's value never fills another's row. With tolerance a carried value is used only while time - valuetime <= tolerance, measured against the time of the row the value came from — a stale value is never evicted, only declined. As in asofjoin, the input pipeline then runs over the widened context [start - tolerance, stop) so rows near start can be filled from before the window, and rows before start are dropped before anything is emitted; this requires the time type to support subtraction (numbers and Dates types do). Without tolerance the input sees only [start, stop), so rows near start may find no earlier value to carry.\n\ntime may not be a key and key columns must be unique — both ArgumentErrors when the transform is built — and the key columns must be present in the input, checked on its first chunk. Element types may differ from chunk to chunk, as everywhere else, and the carried state tracks their promotion; the set of columns being filled may not, and a chunk that changes it is an ArgumentError.\n\nforwardfill is causal — the value at time t came from a row with time <= t — and stateful across chunk boundaries, so streaming a pipeline agrees with loading it.\n\np |> forwardfill(:bid, :ask; key = :sym, tolerance = Minute(5))\n\nSee fillmissing to fill with a constant instead. The curried form composes with |>; the uncurried form applies directly, so forwardfill(p, :bid; key) is equivalent to p |> forwardfill(:bid; key).\n\n\n\n\n\n","title":"CausalFrames.forwardfill"},{"category":"function","location":"api/columns/#CausalFrames.fillmissing","page":"Column transformations","text":"fillmissing(specs...) -> (CausalPipeline -> CausalPipeline)\nfillmissing(p::CausalPipeline, specs...) -> CausalPipeline\n\nA transform replacing missing in the named columns with a per-column constant. The spec is a NamedTuple, a name => value pair, or a collection of such pairs:\n\np |> fillmissing(:qty => 0.0, :sym => \"\")\np |> fillmissing((qty = 0.0, sym = \"\"))\n\nEvery row and every other column is kept. A filled column's element type becomes promote_type(nonmissingtype(T), typeof(value)) — no Missing, since every one is replaced — and a named column whose type admits no Missing is left untouched. Unlike forwardfill this is row-wise and stateless: the fill value does not depend on any other row.\n\nColumn names must be unique and may not be time, which is the ordering dimension and never missing — both ArgumentErrors when the transform is built. A named column the data does not have is an ArgumentError when the chunk arrives.\n\nThe curried form composes with |>; the uncurried form applies directly, so fillmissing(p, :qty => 0.0) is equivalent to p |> fillmissing(:qty => 0.0).\n\n\n\n\n\n","title":"CausalFrames.fillmissing"},{"category":"function","location":"api/columns/#CausalFrames.asofjoin","page":"Column transformations","text":"asofjoin(right::CausalPipeline; key = nothing, tolerance = nothing,\n         strict = false, leftprefix = nothing, rightprefix = nothing,\n         righttime = nothing) -> (CausalPipeline -> CausalPipeline)\nasofjoin(left::CausalPipeline, right::CausalPipeline; key = nothing,\n         ...) -> CausalPipeline\n\nA transform joining each left row to the most recent right row whose time is not after the left row's time (strict = true: strictly before). Every left row is kept; the right table's value columns are appended with element type Union{Missing, T} and are missing where no right row qualifies. Among right rows sharing one time, the last in stream order wins. The right time column is dropped unless righttime names an output column for the matched row's time.\n\nWith key (a column name or collection of column names, present in both tables) rows join per unique key value, exact-matched; the key columns appear once in the output, taken from the left row, never prefixed. With tolerance the match additionally requires time - rtime <= tolerance, and the right pipeline runs over the widened context [start - tolerance, stop) so lookback near the window start is fully covered — this requires the time type to support subtraction (numbers and Dates types do). Without tolerance the right pipeline sees only [start, stop), so left rows near start may find no earlier right row.\n\nleftprefix / rightprefix rename that side's non-time, non-key columns to \"{prefix}_{name}\". Output column names must be unique after prefixing — in particular a self join (p |> asofjoin(p)) needs a prefix. A right stream producing no chunks passes left chunks through unchanged (no right columns, no righttime), except for the leftprefix rename.\n\nA right pipeline whose rows all share one time is a plain keyed lookup; see the manual's Recipes page, which also covers the way that fails when the time falls outside the window.\n\nThe curried form composes with |>; the uncurried form applies directly, so asofjoin(left, right; ...) is equivalent to left |> asofjoin(right; ...).\n\n\n\n\n\n","title":"CausalFrames.asofjoin"},{"category":"function","location":"api/columns/#CausalFrames.Acausal.futurejoin","page":"Column transformations","text":"futurejoin(right::CausalPipeline; key = nothing, tolerance = nothing,\n           strict = false, leftprefix = nothing, rightprefix = nothing,\n           righttime = nothing) -> (CausalPipeline -> CausalPipeline)\nfuturejoin(left::CausalPipeline, right::CausalPipeline; key = nothing,\n           ...) -> CausalPipeline\n\nThe forward-looking mirror of asofjoin: a transform joining each left row to the earliest right row whose time is not before the left row's time (strict = true: strictly after). Every left row is kept; the right table's value columns are appended with element type Union{Missing, T} and are missing where no right row qualifies. Among right rows sharing one time, the first in stream order wins. The right time column is dropped unless righttime names an output column for the matched row's time.\n\nThis operator is acausal: a row emitted at time t looks at right rows with time >= t. That is why it is segregated in the CausalFrames.Acausal submodule and must be opted into explicitly (using CausalFrames.Acausal); everything the top-level module exports stays causal.\n\nWith key (a column name or collection of column names, present in both tables) rows join per unique key value, exact-matched; the key columns appear once in the output, taken from the left row, never prefixed. With tolerance the match additionally requires rtime - time <= tolerance, and the right pipeline runs over the widened context [start, stop + tolerance) so lookahead past the window end is covered — this requires the time type to support addition (numbers and Dates types do). Without tolerance the right pipeline sees only [start, stop), so left rows near stop may find no later right row.\n\nleftprefix / rightprefix rename that side's non-time, non-key columns to \"{prefix}_{name}\". Output column names must be unique after prefixing — in particular a self join (p |> futurejoin(p)) needs a prefix. A right stream producing no chunks passes left chunks through unchanged (no right columns, no righttime), except for the leftprefix rename.\n\nBecause the match is the earliest qualifying right row, buffering is unavoidable: right rows are held per key until a left row consumes or outruns them, and confirming that a key has no future match drains the right stream. Worst-case memory is therefore O(number of right rows), unlike asofjoin's O(number of keys) store — the price of looking forward.\n\nThe curried form composes with |>; the uncurried form applies directly, so futurejoin(left, right; ...) is equivalent to left |> futurejoin(right; ...).\n\n\n\n\n\n","title":"CausalFrames.Acausal.futurejoin"},{"category":"section","location":"api/io/#File-I/O","page":"File I/O","text":"The operators that move rows between a pipeline and a file. Reading starts a pipeline; writing does not end one — every writer is a pass-through transform, so a sink can sit in the middle of a chain.\n\nParquet support is optional, through two backends: either using DuckDB or using Parquet2 enables both parquet operators. Reading prefers DuckDB (it pushes the window into the reader) and writing prefers Parquet2 (it streams row groups out); backend = :duckdb / :parquet2 forces the choice.","title":"File I/O"},{"category":"section","location":"api/io/#Reading","page":"File I/O","text":"Every reader clips what it produces to the context's half-open interval [start, stop), and reads incrementally rather than loading the file.","title":"Reading"},{"category":"section","location":"api/io/#readcsv","page":"File I/O","text":"","title":"readcsv"},{"category":"section","location":"api/io/#readparquet","page":"File I/O","text":"","title":"readparquet"},{"category":"section","location":"api/io/#readjls","page":"File I/O","text":"","title":"readjls"},{"category":"section","location":"api/io/#Writing","page":"File I/O","text":"Every sink is a pass-through transform: it writes each chunk as it flows by and yields it downstream unchanged. Pair one with scan to drive a pipeline for its side effects alone.","title":"Writing"},{"category":"section","location":"api/io/#writecsv","page":"File I/O","text":"","title":"writecsv"},{"category":"section","location":"api/io/#writeparquet","page":"File I/O","text":"","title":"writeparquet"},{"category":"section","location":"api/io/#writejls","page":"File I/O","text":"","title":"writejls"},{"category":"function","location":"api/io/#CausalFrames.readcsv","page":"File I/O","text":"readcsv(path; types = nothing, time = nothing, rename = nothing,\n        delim = nothing, chunkbytes = 4 * 1024 * 1024) -> CausalPipeline\n\nA source that reads the CSV file at path and clips it to the context's half-open interval [start, stop). Every column is read as String — types are not inferred — unless types opts a column into a concrete type.\n\nThe resulting time column, whatever its source, is materialized as :time, must be sorted in non-decreasing order, and is converted to the context's time type. It is chosen by time:\n\ntime = nothing (default): the column already named :time.\ntime = :name (a Symbol): the column named :name (after rename), renamed to :time.\ntime = f (a function): f(row) is called per row to compute the time value, producing the :time column (any existing :time is overwritten).\n\nSince a String time column cannot be ordered against the numeric window, the time column must be typed unless produced by a function: it is an ArgumentError if time is not a function and types gives no concrete type for the time column.\n\nKeyword arguments:\n\ntypes: which columns to give a concrete type (everything else stays String), as a Dict/Vector/function over the file's original column names or indices — it is applied while parsing, so it is keyed by the names in the file, before any rename.\nrename: an AbstractDict/map (over original names) or a name -> name function applied to the column names after typing but before time is resolved.\ndelim: the field delimiter, passed through to CSV.Chunks (a Char or String); defaults to CSV.jl's own detection.\nchunkbytes: the file is read incrementally in chunks of roughly this many bytes — never all at once — and reading stops as soon as a time >= stop is seen. Consequently a sortedness violation is only detected when the offending chunk is actually read.\n\n\n\n\n\n","title":"CausalFrames.readcsv"},{"category":"function","location":"api/io/#CausalFrames.readparquet","page":"File I/O","text":"readparquet(path; time = nothing, rename = nothing, backend = :auto) ->\n    CausalPipeline\n\nA source that reads the parquet file at path and clips it to the context's half-open interval [start, stop). Needs one of the two parquet backends loaded — using DuckDB or using Parquet2 — and uses DuckDB when both are.\n\nParquet is self-describing, so — unlike readcsv — there is nothing to type by hand. The file is read in chunks, never all at once, and the context window is used to skip data that cannot be in it:\n\nbackend Chunk Window\n:duckdb (preferred) a DuckDB result chunk pushed into the reader: row groups and pages outside the window are never decoded\n:parquet2 one row group row groups whose recorded time statistics fall outside the window are skipped undecoded\n\nThe resulting time column, whatever its source, is materialized as :time, must be sorted in non-decreasing order, and is converted to the context's time type. It is chosen by time:\n\ntime = nothing (default): the column already named :time.\ntime = :name (a Symbol): the column named :name (after rename), renamed to :time.\ntime = f (a function): f(row) is called per row to compute the time value, producing the :time column (any existing :time is overwritten).\n\nKeyword arguments:\n\nrename: an AbstractDict/map (over the file's own names) or a name -> name function applied to the column names before time is resolved.\nbackend: :auto (default), :duckdb or :parquet2. Naming a backend that is not loaded is an ArgumentError.\n\nSkipping applies only when the time values come from a real column: a time function is opaque to the reader, so such a file is scanned from the start (it still stops as soon as a time >= stop is seen). It never changes results — the rows are clipped again on arrival — so a file whose writer recorded no statistics simply reads more of itself. Consequently, as with readcsv, a sortedness violation is only detected in the chunks actually read.\n\n\n\n\n\n","title":"CausalFrames.readparquet"},{"category":"function","location":"api/io/#CausalFrames.readjls","page":"File I/O","text":"readjls(path) -> CausalPipeline\n\nA source reading a file written by writejls: one chunk per record, each clipped to the context's [start, stop), with the time converted to the context's time type. Reading is incremental — a record at a time, never the whole file — and stops as soon as a time >= stop is seen, but there is no index to seek by, so a read costs the file's prefix up to stop. As for readcsv, sortedness is checked in the records actually read.\n\nA file that was not written by writejls is an ArgumentError, as is one whose last record was cut short by an interrupted write (every record before it has already been emitted). Deserialization can construct arbitrary types, so read only files you trust.\n\n\n\n\n\n","title":"CausalFrames.readjls"},{"category":"function","location":"api/io/#CausalFrames.writecsv","page":"File I/O","text":"writecsv(path; queue = 1, kwargs...) -> (CausalPipeline -> CausalPipeline)\nwritecsv(p::CausalPipeline, path; ...) -> CausalPipeline\n\nA transparent pass-through transform that writes the stream to the CSV file at path as it flows by, yielding every chunk downstream unchanged. Nothing is buffered: each chunk is written and flushed as it is produced, so the file grows while the pipeline is still running.\n\nWriting happens on a background task fed by a bounded queue, so the pipeline does not block on disk I/O — only if the writer falls more than queue chunks behind, plus once at the end to join it. queue = 0 makes each hand-off a rendezvous.\n\nThe file is truncated when the run starts and finalized when the stream is exhausted — by load, scan, or a fully drained stream. Abandoning a stream part-way leaves the last chunks unwritten; use scan when the file is all you want:\n\nscan(ctx, readcsv(\"ticks.csv\"; types = tt) |>\n          addcolumns(r -> (; mid = (r.bid + r.ask) / 2)) |>\n          writecsv(\"mids.csv\"))\n\nA stream with no rows at all yields an empty file. Keyword arguments are passed through to CSV.write (delim, missingstring, dateformat, quotestrings, bufsize, …), except for append, header, writeheader, partition and compress, which this transform controls itself — passing one is an ArgumentError.\n\nThe curried form composes with |>; the uncurried form applies directly, so writecsv(p, path) is equivalent to p |> writecsv(path).\n\n\n\n\n\n","title":"CausalFrames.writecsv"},{"category":"function","location":"api/io/#CausalFrames.writeparquet","page":"File I/O","text":"writeparquet(path; queue = 1, rowgroupsize = 1_000_000, backend = :auto,\n             kwargs...) -> (CausalPipeline -> CausalPipeline)\nwriteparquet(p::CausalPipeline, path; ...) -> CausalPipeline\n\nA transparent pass-through transform that writes the stream to the parquet file at path as it flows by, yielding every chunk downstream unchanged. Needs one of the two parquet backends loaded — using Parquet2 or using DuckDB — and uses Parquet2 when both are.\n\nLike writecsv, writing happens on a background task fed by a bounded queue, so the pipeline does not block on disk I/O — only if the writer falls more than queue chunks behind, plus once at the end to join it. Chunks are only ever merged, never split, so rowgroupsize = 1 writes one row group per incoming chunk. How they reach the file depends on the backend:\n\nbackend Writing Memory\n:parquet2 (preferred) one row group per rowgroupsize buffered rows, written as the stream flows by bounded by rowgroupsize\n:duckdb chunks are staged in a temporary DuckDB table and written by a single COPY when the stream ends scales with the whole output (DuckDB spills to its temp directory)\n\nrowgroupsize is exact under Parquet2 and a hint under DuckDB, which rounds it up to a multiple of its own 2048-row vector size.\n\nUnlike a CSV file, a parquet file is only valid once finalized, which happens when the stream is exhausted — by load, scan, or a fully drained stream. There is no usable prefix on disk while the run is in flight, and abandoning a stream part-way leaves an unusable file; use scan when the file is all you want:\n\nscan(ctx, readparquet(\"ticks.parquet\") |>\n          addcolumns(r -> (; mid = (r.bid + r.ask) / 2)) |>\n          writeparquet(\"mids.parquet\"))\n\nA stream with no rows at all yields a valid file with no rows.\n\nbackend is :auto (default), :parquet2 or :duckdb; naming one that is not loaded is an ArgumentError. Remaining keyword arguments are passed through to Parquet2.FileWriter (compression_codec, npages, metadata, column_metadata, …), where compute_statistics defaults to [\"time\"] so that files written here carry the statistics readparquet skips by. The DuckDB backend understands compression_codec (:zstd, :snappy, :gzip, :uncompressed) and records statistics of its own, but rejects the other, Parquet2-specific options.\n\nThe curried form composes with |>; the uncurried form applies directly, so writeparquet(p, path) is equivalent to p |> writeparquet(path).\n\n\n\n\n\n","title":"CausalFrames.writeparquet"},{"category":"function","location":"api/io/#CausalFrames.writejls","page":"File I/O","text":"writejls(path; queue = 1) -> (CausalPipeline -> CausalPipeline)\nwritejls(p::CausalPipeline, path; queue = 1) -> CausalPipeline\n\nA transparent pass-through transform that writes the stream to path through Julia's Serialization stdlib as it flows by, yielding every chunk downstream unchanged — writecsv's contract in a format that stores any Julia value a column holds, so columns that neither CSV nor parquet can encode (a fitted model, a NamedTuple) round-trip through readjls.\n\nEach chunk becomes one serialized record, written and flushed as it is produced on a background task fed by a bounded queue of depth queue, exactly as for writecsv. The file is truncated when the run starts and finalized when the stream is exhausted; a stream with no rows yields a file holding only its header, which reads back as an empty stream. Every complete record of an interrupted run stays readable.\n\nA JLS file is readable only by a compatible Julia and compatible versions of the packages whose types it holds, so it is a persistence format for a pipeline's own outputs, not an interchange format.\n\nThe curried form composes with |>; the uncurried form applies directly, so writejls(p, path) is equivalent to p |> writejls(path).\n\n\n\n\n\n","title":"CausalFrames.writejls"},{"category":"section","location":"api/summarizing/#Summarizing-transforms","page":"Summarizing transforms","text":"Each of these folds one or more summarizers over a set of rows — the whole window, one timestamp, an interval, a trailing window — and an optional key splits the fold per unique key value.","title":"Summarizing transforms"},{"category":"section","location":"api/summarizing/#summarize","page":"Summarizing transforms","text":"","title":"summarize"},{"category":"section","location":"api/summarizing/#summarizecycles","page":"Summarizing transforms","text":"","title":"summarizecycles"},{"category":"section","location":"api/summarizing/#intervalize","page":"Summarizing transforms","text":"","title":"intervalize"},{"category":"section","location":"api/summarizing/#summarizewindows","page":"Summarizing transforms","text":"","title":"summarizewindows"},{"category":"section","location":"api/summarizing/#addsummarycolumns","page":"Summarizing transforms","text":"","title":"addsummarycolumns"},{"category":"section","location":"api/summarizing/#addrollingcolumns","page":"Summarizing transforms","text":"","title":"addrollingcolumns"},{"category":"function","location":"api/summarizing/#CausalFrames.summarize","page":"Summarizing transforms","text":"summarize(summarizers; key = nothing) -> (CausalPipeline -> CausalPipeline)\nsummarize(p::CausalPipeline, summarizers; key = nothing) -> CausalPipeline\n\nA transform summarizing the whole context: every input row is folded into the summarizers and a single batch of rows is emitted at the context's end time stop, dropping the input columns. summarizers is a Summarizer or a collection of them; the output columns are time, the key columns, then each summarizer's value columns.\n\nWithout key the output is exactly one row (the identity summary — e.g. count = 0 — when the input is empty). With key (a column name or collection of column names) one row is emitted per unique key value, sorted by key; an empty input yields no rows.\n\nThe curried form composes with |>; the uncurried form applies directly, so summarize(p, ss; key) is equivalent to p |> summarize(ss; key).\n\n\n\n\n\n","title":"CausalFrames.summarize"},{"category":"function","location":"api/summarizing/#CausalFrames.summarizecycles","page":"Summarizing transforms","text":"summarizecycles(summarizers; key = nothing) -> (CausalPipeline -> CausalPipeline)\nsummarizecycles(p::CausalPipeline, summarizers; key = nothing) -> CausalPipeline\n\nA transform summarizing each cycle — a maximal run of rows sharing one timestamp — independently, with fresh state per cycle. For every cycle one row is emitted at the cycle's time (per unique key value, sorted by key, when key is given), dropping the input columns. A cycle spanning a chunk boundary is summarized as a single cycle.\n\nThe curried form composes with |>; the uncurried form applies directly, so summarizecycles(p, ss; key) is equivalent to p |> summarizecycles(ss; key).\n\n\n\n\n\n","title":"CausalFrames.summarizecycles"},{"category":"function","location":"api/summarizing/#CausalFrames.intervalize","page":"Summarizing transforms","text":"intervalize(clock, summarizers; key = nothing,\n            closelast = false) -> (CausalPipeline -> CausalPipeline)\nintervalize(p::CausalPipeline, clock, summarizers; key = nothing,\n            closelast = false) -> CausalPipeline\n\nA transform summarizing the data stream over the intervals defined by a clock pipeline: the clock's :time column supplies boundaries b₀ < b₁ < … < b_K, and each complete interval [bₖ, bₖ₊₁) — inclusive of its begin, exclusive of its end — is summarized independently, dropping the input columns. Everything after clock mirrors summarize: summarizers is a Summarizer or a collection of them, and each summary is emitted at the interval's end time bₖ₊₁, with columns time, the key columns, then each summarizer's value columns. Only the clock's :time column is used; any other columns it carries are ignored. Input rows before b₀ fall in no interval and are dropped.\n\nWithout key the output is a regular grid: every complete interval emits exactly one row, an empty interval (no input rows) included with the summarizers' identity/missing values (count = 0, sum = 0, mean = missing), so the output-column element types widen to admit them (e.g. Mean gives Union{Missing, Float64}). With key (a column name or collection of column names) the output is sparse: unseen keys cannot be emitted causally, so each interval emits one row per key present in it, sorted by key, and an interval with no rows emits nothing.\n\nThe trailing partial interval [b_K, stop) after the final boundary is emitted only when closelast = true, timestamped at the context end stop; otherwise it is dropped. An empty clock produces no output.\n\nThe curried form composes with |>; the uncurried form applies directly, so intervalize(p, clock, ss; …) is equivalent to p |> intervalize(clock, ss; …).\n\n\n\n\n\n","title":"CausalFrames.intervalize"},{"category":"function","location":"api/summarizing/#CausalFrames.summarizewindows","page":"Summarizing transforms","text":"summarizewindows(clock, lookback, summarizers;\n                 key = nothing) -> (CausalPipeline -> CausalPipeline)\nsummarizewindows(p::CausalPipeline, clock, lookback, summarizers;\n                 key = nothing) -> CausalPipeline\n\nA transform summarizing a trailing window at every tick of a clock pipeline: at each time τ in the clock's :time column, the rows with time in [τ - lookback, τ) — inclusive of the window's start, exclusive of the tick itself — are summarized and emitted at τ, dropping the input columns. summarizers is a Summarizer or a collection of them; the output columns are time, the key columns, then each summarizer's value columns. Only the clock's :time column is used.\n\nIt generalizes intervalize: intervalize's intervals are the gaps between consecutive ticks, while these windows have a fixed length that may be shorter or longer than the gaps, so windows may overlap. A lookback equal to a regular clock's spacing reproduces intervalize's intervals. The input runs over the context widened to [start - lookback, stop), so the first tick already sees a full window; this requires the time type to support subtraction (numbers and Dates types do), and lookback must be non-negative.\n\nWithout key the output is a regular grid: every tick emits exactly one row, an empty window included, with the summarizers' identity/missing values (count = 0, mean = missing), so element types widen to admit them. With key (a column name or collection of column names) the output is sparse: each tick emits one row per key with rows in its window, sorted by key — plus one row of empty values for each key that had rows in its window at the previous tick and has none now. That row is what lets a consumer tracking the latest row per key (such as asofjoin) see a key's summary go empty rather than keep its stale value; the key is then not emitted again until its window holds rows.\n\nWindows over GroupSummarizers slide in O(1) per row, subtracting rows as they leave. Windows over MonoidSummarizers (Min, First, …) fold from a segment tree of partial combinations, O(1) amortized per row plus O(log window) per key per tick. Anything else is re-folded over each window, O(window) per tick.\n\nThe curried form composes with |>; the uncurried form applies directly, so summarizewindows(p, clock, lookback, ss; key) is equivalent to p |> summarizewindows(clock, lookback, ss; key).\n\n\n\n\n\n","title":"CausalFrames.summarizewindows"},{"category":"function","location":"api/summarizing/#CausalFrames.addsummarycolumns","page":"Summarizing transforms","text":"addsummarycolumns(summarizers; key = nothing) -> (CausalPipeline -> CausalPipeline)\naddsummarycolumns(p::CausalPipeline, summarizers; key = nothing) -> CausalPipeline\n\nA transform keeping all existing columns and appending each summarizer's value columns, holding the running summary after that row has been folded in. With key (a column name or collection of column names) a separate running summary is kept per unique key value. State runs over the whole window, carried across chunk boundaries. The output columns may not collide with existing columns.\n\nA keyed Count() is therefore a per-key row number; see the manual's Recipes page for when that amounts to an arbitrary ORDER BY.\n\nThe curried form composes with |>; the uncurried form applies directly, so addsummarycolumns(p, ss; key) is equivalent to p |> addsummarycolumns(ss; key).\n\n\n\n\n\n","title":"CausalFrames.addsummarycolumns"},{"category":"function","location":"api/summarizing/#CausalFrames.addrollingcolumns","page":"Summarizing transforms","text":"addrollingcolumns(windows, summarizers; key = nothing,\n                  from = nothing) -> (CausalPipeline -> CausalPipeline)\naddrollingcolumns(p::CausalPipeline, windows, summarizers;\n                  ...) -> CausalPipeline\n\nA transform keeping all existing columns and appending, for each named window, each summarizer's value columns computed over that row's trailing window: the summarized rows with time s satisfying s <= t and t - s <= lookback, inclusive on both ends, for an output row at time t. windows maps window names to look-backs — a NamedTuple ((m5 = Minute(5), h1 = Hour(1))), a single name => lookback pair, or a collection of pairs — and each summary column is named by prefixing the window name: m5_price_sum. Look-backs must be non-negative; as for asofjoin tolerance, the time type must support subtraction (numbers and Dates types do).\n\nBy default the summaries are computed over the pipeline being augmented, which then runs twice — once for the rows, once for the summaries. from names a different pipeline to summarize instead; its rows relate to the output rows by time (and key) only, never by row identity. Either way the summarized pipeline runs over the widened context [minimum over windows of start - lookback, stop), so windows reaching before start are fully covered and the first output row already sees a full look-back of history.\n\nWith key (a column name or collection of column names, present in both inputs) each row's window holds only the summarized rows sharing its key value. A window holding no rows — including a key never seen — yields the summarizers' empty values, so an output column's element type widens accordingly (e.g. Min gives Union{Missing, T}). A summarized stream producing no chunks yields the empty values everywhere. The output columns may not collide with existing columns.\n\nThe curried form composes with |>; the uncurried form applies directly, so addrollingcolumns(p, w, ss; ...) is equivalent to p |> addrollingcolumns(w, ss; ...).\n\n\n\n\n\n","title":"CausalFrames.addrollingcolumns"},{"category":"section","location":"#CausalFrames.jl","page":"Home","text":"Time-series tables for Julia: DataFrames with a monotonically non-decreasing time column, built lazily from composable pipelines.\n\nusing CausalFrames, Dates\n\np = readcsv(\"ticks.csv\";\n        types = Dict(:time => DateTime, :bid => Float64, :ask => Float64)) |>\n    filterrows(r -> r.bid > 0) |>\n    addcolumns(r -> (; mid = (r.bid + r.ask) / 2))\n\nframe = load(Context(DateTime(2026, 1, 1), DateTime(2026, 2, 1)), p)","title":"CausalFrames.jl"},{"category":"section","location":"#Concepts","page":"Home","text":"Context(start, stop) — a time window. The time type is generic: DateTime, Int ticks, Float64 seconds, anything ordered.\nCausalPipeline — a lazy description of how to produce data; conceptually a function from a Context to a stream of chunks. Built from sources and chained with |>. Evaluation is streaming end to end: load(ctx, pipeline) materializes the whole window (the only operation that does), stream(ctx, pipeline) yields frames incrementally, and scan(ctx, pipeline) runs a pipeline for its side effects, discarding every chunk.\nCausalFrame — a materialized table. It hides its backing storage (one or more time-disjoint chunks, wrapped without copying) and is accessed via the Tables.jl interface or DataFrame(frame).","title":"Concepts"},{"category":"section","location":"#Operators","page":"Home","text":"Each operator below links to its API reference. Every transform also has an uncurried, pipeline-first form — filterrows(p, pred), addcolumns(p, f), summarize(p, ss; key) — equivalent to the |> chain (p |> filterrows(pred)) for when the applied form reads clearer.\n\nRow functions receive a map-like row object: row.time, row.price, row[:price].","title":"Operators"},{"category":"section","location":"#Sources","page":"Home","text":"A source starts a pipeline, constructing or combining streams without touching a file.\n\nOperator Semantics\nemptyframe() zero rows, just a :time column\nconcatenate(ps...) run the pipelines one after another, emitting their chunks end to end; they must be passed in time order and produce identical column names\nmerge(ps...; batchsize) run the pipelines concurrently and interleave their rows by time; the output carries the union of their columns, missing where a pipeline lacks one, ties broken by argument order\nclock(interval) one row per interval in [start, stop)","title":"Sources"},{"category":"section","location":"#File-I/O","page":"Home","text":"Reading starts a pipeline; writing does not end one — every writer is a pass-through transform, so a sink can sit in the middle of a chain.\n\nOperator Semantics\nreadcsv(path; types, time, rename, delim) CSV read as String columns (types opts columns into concrete types); time picks the time column by name or a per-row function; clipped to [start, stop), read incrementally\nreadparquet(path; time, rename, backend) parquet file (needs using DuckDB or using Parquet2); types come from the file; the context window skips row groups that cannot be in it; time and rename as for readcsv\nreadjls(path) read back a file written by writejls, a record at a time, clipped to [start, stop)\nwritecsv(path; queue, ...) writes each chunk to path as it flows by, on a background task\nwriteparquet(path; queue, rowgroupsize, backend, ...) needs using Parquet2 or using DuckDB: writes row groups of rowgroupsize rows as the stream flows by; the file is valid only once the stream is exhausted\nwritejls(path; queue) through Julia's Serialization stdlib: one record per chunk, so columns CSV and parquet cannot encode (fitted models, NamedTuples) round-trip; tied to the Julia and package versions that wrote it\n\nParquet support is optional, through two backends: either using DuckDB or using Parquet2 enables both operators. Reading prefers DuckDB (it pushes the window into the reader) and writing prefers Parquet2 (it streams row groups out); backend = :duckdb / :parquet2 forces the choice.\n\nwritecsv streams a pipeline to disk without materializing it, and scan drives the pipeline for its side effects alone. load, stream and scan also take a curried, context-only form, so a chain can end in its own evaluation:\n\nreadcsv(\"ticks.csv\"; types = Dict(:time => Int, :bid => Float64)) |>\n    filterrows(r -> r.bid > 0) |>\n    writecsv(\"clean.csv\") |>\n    scan(Context(0, 10^6))","title":"File I/O"},{"category":"section","location":"#Row-transformations","page":"Home","text":"Driven by one row at a time: testing a row, computing values from it, and selecting or retiming rows.\n\nOperator Semantics\nfilterrows(pred) keep rows where pred(row)\naddcolumns(f) f(row)::NamedTuple of new column values\nhead(n) emit the first up to n rows, then stop pulling the source — it genuinely stops, so a readcsv behind head(10) reads one file chunk\nlastrow(; key) emit the last row, or one per key, retimed to the window's stop\nlag(offset) shift every row offset later in time, so time t carries what the input had at t - offset; only :time changes and offset must be non-negative\nsettime(spec) recompute :time from a column name or a per-row function; rows may only move later, and the result is re-clipped to [start, stop)\n\nThe forward-looking counterparts — lead, and a permissive settime — live in the Acausal submodule, under \"Causality\" below.","title":"Row transformations"},{"category":"section","location":"#Column-transformations","page":"Home","text":"The column set itself: which columns exist and in what order, and filling or joining values into them. Rows pass through one for one.\n\nOperator Semantics\nselectcolumns(sel...) keep the columns matching a name, Regex, name predicate, or collection of those (:time always kept)\ndropcolumns(sel...) drop the columns matching the same selector forms (:time never dropped)\nreordercolumns(sel...) move the matching columns to the front, in the selectors' order, the rest following in the input's order (:time always first)\nforwardfill(sel...; key, tolerance) replace missing in the selected columns with that column's last non-missing value, per key, while not older than tolerance\nfillmissing(specs...) replace missing with a per-column constant, given as name => value pairs or a NamedTuple\nasofjoin(right; key, tolerance, ...) append the most recent right-pipeline row at or before each row's time\n\nThe forward-looking futurejoin lives in the Acausal submodule, under \"Causality\" below.","title":"Column transformations"},{"category":"section","location":"#Summarizing-transforms","page":"Home","text":"Each folds one or more summarizers over a set of rows.\n\nOperator Semantics\nsummarize(ss; key) summarize the whole window into rows at time stop\nsummarizecycles(ss; key) summarize each unique timestamp independently\nintervalize(clock, ss; key, closelast) summarize over the intervals [bₖ, bₖ₊₁) a clock pipeline's times define, each emitted at its end time\nsummarizewindows(clock, lookback, ss; key) at each tick τ of a clock pipeline, summarize the trailing window [τ - lookback, τ); one row per tick, or per key with rows in its window (plus one empty row when a key's window empties)\naddsummarycolumns(ss; key) append running summary values after each row\naddrollingcolumns(windows, ss; key, from) append summaries over named trailing windows, columns prefixed {window}_","title":"Summarizing transforms"},{"category":"section","location":"#Model-fitting-(MLJ)","page":"Home","text":"Operator Semantics\napplymodels(models; column, key, tolerance, strict, name, operation) append each row's prediction from the latest fitted model at or before it (per key), given a pipeline of FitModel outputs (needs using MLJ)\naddpredictions(clock, lookback, model, predictors, response; key, ...) refit an MLJ model at every clock tick τ over [τ - lookback, τ), per key, and append each row's prediction from the latest model — training rows always precede the rows they predict\nmodelreports(; column, name) over a pipeline of fitted models: replace each model with its fit report","title":"Model fitting (MLJ)"},{"category":"section","location":"#Summarizers","page":"Home","text":"The summarization transforms take one or more summarizers — or your own Summarizer subtype — and an optional key (one or more column names) to produce a separate summary per unique key value. Output columns are named by suffix, shown here for a column :x (and :y, for the pairwise ones):\n\nSummarizer Output Structure Semantics\nCount() :count group rows summarized; 0 for no rows\nCountDistinct(:x) :x_countdistinct monoid distinct values seen, always an Int, missing counted as one of them; 0 for no rows\nSum(:x) :x_sum group Σx, widening as Base.sum does; 0 for no rows\nSumPower(:x, n) :x_sumpower_n group Σxⁿ; 0 for no rows\nProduct(:x) :x_product monoid Πx; 1 for no rows\nDotProduct(:x, :y) :x_y_dotproduct group Σxy; 0 for no rows\nMoment(:x, n) :x_moment_n group the n-th raw moment, Σxⁿ / count\nMean(:x) :x_mean group arithmetic mean\nVariance(:x; corrected) :x_variance group variance, following Statistics.var\nStd(:x; corrected) :x_std group standard deviation, following Statistics.std\nCovariance(:x, :y; corrected) :x_y_covariance group covariance, following Statistics.cov\nCorrelation(:x, :y) :x_y_correlation group Pearson correlation\nLinearRegression(predictors, response; name, intercept) a block of columns under the name prefix group ordinary least squares; see below\nMin(:x) :x_min monoid smallest value\nMax(:x) :x_max monoid largest value\nFirst(:x) :x_first monoid value of the earliest row\nLast(:x) :x_last monoid value of the latest row\nFitModel(model, predictors, response) :model — fits model to the rows it summarizes (needs using MLJ)\n\nOnly the four accumulating summarizers and CountDistinct have an identity element, given in the table; the rest summarize no rows as missing. CountDistinct is also the one summarizer a missing does not poison — unlike a sum, a distinct count stays knowable — and the one whose state is not O(1), holding the distinct values it has seen. (MLJ exports a scientific type named Count, so alongside using MLJ the summarizer is CausalFrames.Count().)\n\np = readcsv(\"ticks.csv\";\n        types = Dict(:time => Int, :bid => Float64, :ask => Float64)) |>\n    addcolumns(r -> (; mid = (r.bid + r.ask) / 2)) |>\n    addsummarycolumns([Count(), Sum(:mid), Min(:mid), Max(:mid)]; key = :symbol)\n\nMoment, Mean, Variance, Std, Covariance, Correlation, and LinearRegression are dependent summarizers, computed from the counting and accumulating ones — Moment(:mid, n) from Count() and SumPower(:mid, n), for instance. Those are folded alongside them but appear in the output only if requested themselves.\n\nFitModel emits the fitted model itself in a :model column — applied to a stream by applymodels, refit on a rolling window by addpredictions, and persisted by writejls.\n\nLinearRegression fits ordinary least squares of a response on one or more predictors, emitting a coefficient and a t statistic per term alongside :r2, :stderr (the residual standard error), and :n:\n\np |> addrollingcolumns((h1 = Hour(1),),\n    [LinearRegression([:mid, :size], :ret; name = :m1)])\n# h1_m1_n, h1_m1_r2, h1_m1_stderr, h1_m1_intercept_beta,\n# h1_m1_intercept_tstat, h1_m1_mid_beta, h1_m1_mid_tstat,\n# h1_m1_size_beta, h1_m1_size_tstat\n\nPass intercept = false to drop the constant term. The name prefix is what lets two regressions share a call — without it they collide on :n, :r2, and :stderr. Being dependent is what makes running several cheap: every regression is a function of the same Count, Sum, SumPower, and DotProduct accumulators, so overlapping predictors fold each cross product exactly once, and a Variance or Correlation requested beside them shares those accumulators too. A window without enough rows, or with collinear predictors, yields NaN rather than raising; :n always reports the honest row count.\n\naddrollingcolumns summarizes named trailing windows instead of the whole window so far: addrollingcolumns((m5 = Minute(5), h1 = Hour(1)), Mean(:mid); key = :symbol) appends m5_mid_mean and h1_mid_mean, each row summarizing the rows within its look-back (t - lookback <= time <= t). By default the pipeline summarizes itself; from names another pipeline to summarize. The summarized pipeline runs over a context widened backward by the longest look-back, so the first row already sees a full window; an empty window yields the summarizer's identity or missing as above.\n\nRolling windows, and the clock-sampled windows of summarizewindows, pick their algorithm from the structure column above: GroupSummarizers (Sum, Mean, …) slide a running state in O(1) per row by subtracting exiting rows, MonoidSummarizers (Min, Product, …) fold each window from a segment tree of partial combinations in O(log window), and summarizers declaring neither re-fold each window from scratch — see DESIGN.md for the combine!/downdate! interface a custom structured summarizer implements. A summarizer on one of these paths should also implement fresh!, which zeroes a state in place: the transforms zero a state tuple per cycle, per interval and per window query, so building a new one there is a heap allocation per summarizer per row.\n\nAn output column takes its element type from the input column: Min, Max, First, and Last reproduce it verbatim, while Sum and SumPower widen it exactly as Base.sum does (Int32 sums to Int64, Float32 to Float32). Summarizers are typed from the input schema, so folding a large window allocates on the order of kilobytes — see DESIGN.md for the interface a custom Summarizer implements.","title":"Summarizers"},{"category":"section","location":"#Causality","page":"Home","text":"Every operator above is causal: its output at time t depends only on input rows with time ≤ t. That gives the chunk-concatenation property — loading [a, c) equals concatenating the results of loading [a, b) and [b, c) — which is what makes chunked, streaming evaluation sound.\n\nThe forward-looking operators are the deliberate exceptions. They live in the Acausal submodule and are never re-exported, so opting into acausality is always explicit:\n\nusing CausalFrames.Acausal\n\nquotes |> futurejoin(fills; key = :symbol)  # earliest fill at or after each quote\nprices |> lead(Minute(5))                   # time -> time - offset\n# the permissive settime, which may move rows earlier:\nprices |> CausalFrames.Acausal.settime(r -> r.exchange_time)\n\nThe permissive settime is the one exception to the submodule's export rule: it is reached only as CausalFrames.Acausal.settime, never unqualified, so that using CausalFrames.Acausal leaves the causal settime usable.\n\nfuturejoin mirrors asofjoin with the match direction inverted — the earliest right row whose time is not before the left row's, missing where none qualifies — and lead mirrors lag; the permissive settime mirrors the causal one. One cost worth knowing before reaching for it: because futurejoin matches the earliest qualifying row, it buffers right rows per key until a left row consumes or outruns them, and proving that a key has no future match drains the right stream. Worst-case memory is therefore O(right rows), against asofjoin's O(keys).\n\nThe Recipes page collects the patterns that are not obvious from the operator list: ranking within a group, joining a reference table, and using a derived stream as one.\n\nSee DESIGN.md for the full design.","title":"Causality"},{"category":"section","location":"api/core/#Frames-and-pipelines","page":"Frames and pipelines","text":"","title":"Frames and pipelines"},{"category":"section","location":"api/core/#Core-types","page":"Frames and pipelines","text":"","title":"Core types"},{"category":"section","location":"api/core/#Evaluation","page":"Frames and pipelines","text":"","title":"Evaluation"},{"category":"section","location":"api/core/#Internals","page":"Frames and pipelines","text":"","title":"Internals"},{"category":"type","location":"api/core/#CausalFrames.Context","page":"Frames and pipelines","text":"Context(start, stop)\n\nA time window over which a CausalPipeline is evaluated. The time type T may be any ordered type (DateTime, Int, Float64, ...). start <= stop is enforced.\n\nSources clip their output to the half-open interval [start, stop); a materialized CausalFrame may contain rows in the closed interval [start, stop] (intermediate operators may emit a row exactly at stop).\n\n\n\n\n\n","title":"CausalFrames.Context"},{"category":"function","location":"api/core/#CausalFrames.timetype","page":"Frames and pipelines","text":"timetype(ctx::Context{T}) -> T\n\nThe time type of a context.\n\n\n\n\n\n","title":"CausalFrames.timetype"},{"category":"type","location":"api/core/#CausalFrames.CausalFrame","page":"Frames and pipelines","text":"CausalFrame(ctx::Context, chunks)\nCausalFrame(ctx::Context, df::DataFrame)\n\nA materialized time-series table over the window ctx. Backed by one or more time-disjoint DataFrame chunks; the chunk structure is an implementation detail and is not part of the public API.\n\nInvariants, checked at construction:\n\nevery chunk has a :time column with element type <: T;\nall (non-empty) chunks share the same column names (element types may differ between chunks; DataFrame(frame) promotes on concatenation);\ntime is non-decreasing within each chunk and across chunk boundaries;\nall times lie in the closed interval [ctx.start, ctx.stop].\n\nAccess the data via the Tables.jl interface, DataFrame(frame), context, nrow(frame), or names(frame). The constructor takes ownership of the passed DataFrames; do not mutate them afterwards.\n\n\n\n\n\n","title":"CausalFrames.CausalFrame"},{"category":"function","location":"api/core/#CausalFrames.context","page":"Frames and pipelines","text":"context(frame::CausalFrame) -> Context\n\nThe time window this frame was loaded over.\n\n\n\n\n\n","title":"CausalFrames.context"},{"category":"method","location":"api/core/#DataFrames.DataFrame-Tuple{CausalFrame}","page":"Frames and pipelines","text":"DataFrame(frame::CausalFrame)\n\nConcatenate the frame's chunks into a plain DataFrame — an explicit exit from the causal world, and the point where the data is copied. A frame with no rows yields a zero-row DataFrame with only its :time column.\n\n\n\n\n\n","title":"DataFrames.DataFrame"},{"category":"type","location":"api/core/#CausalFrames.CausalPipeline","page":"Frames and pipelines","text":"CausalPipeline(run)\n\nA lazy description of how to produce time-series data: conceptually a function Context -> single-pass lazy iterator of DataFrame chunks (time non-decreasing within and across chunks). Nothing runs until the iterator is consumed; load drains it into a CausalFrame and stream yields one frame per chunk. Build pipelines from sources (emptyframe, clock, readcsv) and chain transforms with |>:\n\np = readcsv(\"ticks.csv\";\n        types = Dict(:time => Int, :bid => Float64, :ask => Float64)) |>\n    filterrows(r -> r.bid > 0) |>\n    addcolumns(r -> (; mid = (r.bid + r.ask) / 2))\n\nEvery operator must be causal: its output at time t may depend only on input rows with time <= t.\n\n\n\n\n\n","title":"CausalFrames.CausalPipeline"},{"category":"function","location":"api/core/#CausalFrames.load","page":"Frames and pipelines","text":"load(ctx::Context, p::CausalPipeline) -> CausalFrame\nload(ctx::Context) -> (CausalPipeline -> CausalFrame)\n\nEvaluate the pipeline over the time window ctx, materializing the whole window into a frame. This is the only operation that forces the full window into memory; the frame wraps the streamed chunks as-is, without copying. An empty result yields a zero-row frame with only a :time column.\n\nThe curried form composes with |>, so a chain can end in its own evaluation: p |> load(ctx) is equivalent to load(ctx, p).\n\n\n\n\n\n","title":"CausalFrames.load"},{"category":"function","location":"api/core/#CausalFrames.stream","page":"Frames and pipelines","text":"stream(ctx::Context, p::CausalPipeline) -> iterator of CausalFrames\nstream(ctx::Context) -> (CausalPipeline -> iterator of CausalFrames)\n\nEvaluate the pipeline over ctx incrementally, yielding one CausalFrame per chunk without ever materializing the whole window. Chunk boundaries are chosen by the pipeline's source (see e.g. the batchsize/chunkbytes arguments of clock and readcsv); stateful operators carry their state across chunks, so concatenating the streamed frames equals load of the whole window.\n\nThe frames' contexts tile [ctx.start, ctx.stop): frame i covers [bᵢ₋₁, bᵢ) where b₀ = ctx.start, bᵢ is the first time of chunk i + 1, and the last frame's context stops at ctx.stop. The iterator is single-pass and maintains one chunk of lookahead.\n\nThe curried form composes with |>, so a chain can end in its own evaluation: p |> stream(ctx) is equivalent to stream(ctx, p).\n\n\n\n\n\n","title":"CausalFrames.stream"},{"category":"function","location":"api/core/#CausalFrames.scan","page":"Frames and pipelines","text":"scan(ctx::Context, p::CausalPipeline) -> Nothing\nscan(ctx::Context) -> (CausalPipeline -> Nothing)\n\nEvaluate the pipeline over ctx, discarding every chunk as it is produced. Nothing is materialized — this is how to run a pipeline for its side effects (see writecsv) without paying for a frame that would be thrown away. Chunks are validated exactly as load validates them.\n\nThe curried form composes with |>, so a chain can end in its own evaluation: p |> scan(ctx) is equivalent to scan(ctx, p).\n\n\n\n\n\n","title":"CausalFrames.scan"},{"category":"function","location":"api/core/#CausalFrames.chunkmap","page":"Frames and pipelines","text":"chunkmap(step, upstream; flush = () -> nothing)\n\nLazy chunk-to-chunk transformer: applies step(chunk::DataFrame) -> Union{Nothing, DataFrame} to each upstream chunk, skipping nothing and empty results; once upstream is exhausted, calls flush() -> Union{Nothing, DataFrame} exactly once and yields its result if non-empty. Single-pass: step and flush may close over mutable per-run state. Internal.\n\n\n\n\n\n","title":"CausalFrames.chunkmap"}]
}
