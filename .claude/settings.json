{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "{ jq -r '.tool_response.filePath // .tool_input.file_path // empty' | { read -r f; case \"$f\" in *.jl) julia -e 'using JuliaFormatter; format(ARGS[1])' -- \"$f\";; esac; }; } 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Formatting Julia"
          }
        ]
      }
    ]
  }
}
